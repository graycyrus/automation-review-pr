# PR #3500 — feat(agent_orchestration): durable workflow-run model + read surface (#3375)

## Metadata
| Field | Value |
|-------|-------|
| Author | @oxoxDev |
| Branch | feat/3375-workflow-runs → main |
| Created | 2026-06-09 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3500 |
| Status | approved |
| Last reviewed commit | 086bd1317092bccc2b2a51d20f6a972ea26ab0a1 |
| Last review date | 2026-06-09T11:31:58Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Adds PR1 of the #3375 epic: a declarative `WorkflowDefinition` phase-graph type, Kahn's-algorithm structural + agent validation, one builtin read-only "parallel research with cross-checking" workflow, and three read-only RPC controllers (`workflow_run_list_definitions`, `workflow_run_list`, `workflow_run_get`) backed by the existing `workflow_runs` ledger table. No execution engine yet — deferred to a follow-up PR. |
| Breaking risk | Low | Purely additive — new module, new public exports from `run_ledger`, new RPC namespace. `get_workflow_run` was a private fn, now public; no callers existed before. |
| Security risk | Zero | All SQL is parameterized. No user input reaches SQL strings. `WorkflowDefinition` is declarative (no arbitrary script execution). No new network dependencies, no secrets. |
| Bottom line | Safe to merge | Clean read-only surface with solid validation and test coverage. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Code is purpose-built; comments explain deferred scope not trivial operations; variable names are precise. |
| Structural signals | None | No over-abstraction, no unnecessary wrappers, no boilerplate patterns. |
| Content signals | None | Meaningful identifiers throughout; no placeholder TODOs; no dead code. |
| Behavioral signals | None | PR scope matches branch name and description; test coverage is substantive (9 unit + 1 e2e). |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — no frontend changes |
| User flow | Unchanged |
| Affected surfaces | N/A |
| Responsiveness | Not applicable |
| Accessibility | Not applicable |
| i18n | Not applicable |
| Performance | Not applicable |
| Edge states | Not applicable |
| Risk | None |
| Recommendation | N/A — backend-only PR |

## Review History

### Review 1 — 2026-06-09T06:59:03Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | cbe212c9d6f55d25395448e62d3b09a74e5f0bff |
| Gates | CI pass | Conflicts pass | Feedback pass |
| Areas changed | Rust core — `agent_orchestration/workflow_runs` (new module), `session_db/run_ledger` (extended), `core/all.rs` (registration), `tests/json_rpc_e2e.rs` |
| Linked issues | #3375 |
| PR-Issue alignment | Issue asks for definition model, one read-only prototype, controller APIs, tests. PR delivers all of those for PR1 scope. Start/stop/resume, UI, and bounded execution deferred to follow-up PRs as documented. Fully aligned. |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds new `workflow_runs` module under `agent_orchestration` with declarative types, structural validation (cycle detection via Kahn's), registry-backed agent validation, one builtin definition, and three read-only RPC controllers over the existing ledger table. Extends `run_ledger` with `list_workflow_runs` and makes `get_workflow_run` public. |
| CodeRabbit dedup | N/A — no prior CodeRabbit review |
| Resolution actions | None — first review |
| Surrounding code checked | Existing `list_agent_runs` in `ops.rs` (parameterized SQL pattern); `all_command_center_registered_controllers` pattern in `mod.rs`; `RpcOutcome` usage in other schema files |
| Dependency audit | No new external dependencies |
| Test coverage | 9 unit tests covering all `DefinitionError` variants + happy path; 1 e2e roundtrip seeding a real ledger row and exercising all three RPC controllers. Coverage gate passed (CI). |
| Impact scan | `get_workflow_run` was previously private — no prior callers. `list_workflow_runs` is new. New RPC namespace `workflow_run` does not conflict with existing `workflows`. |

#### Findings
- [minor] `src/openhuman/agent_orchestration/workflow_runs/schemas.rs:77` — `_ =>` arm in `schema_for()` silently returns a dummy no-op `ControllerSchema`. Replace with `unreachable!()` so typos in string literals are caught immediately.
- [minor] `src/openhuman/session_db/run_ledger/types.rs:277` — `limit`/`offset` fields are `Option<u32>` but controller schema advertises `TypeSchema::U64`. Inconsistent; should be aligned.
- [minor] `src/openhuman/agent_orchestration/workflow_runs/ops.rs` — `validate_structure` doesn't validate `default_concurrency` or `max_children`; zero values accepted silently. Worth adding a `DefinitionError::InvalidConcurrency` variant before the execution engine lands.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | APPROVE — CI 100% green, no critical or major issues, solid test coverage, clean implementation |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3500#pullrequestreview-4456034059 |

### Review 2 — 2026-06-09T11:31:58Z

#### Context
| Field | Value |
|-------|-------|
| Type | Continuation (prior `graycyrus` review dismissed after all CodeRabbit changes addressed) |
| Commit | 086bd1317092bccc2b2a51d20f6a972ea26ab0a1 |
| Gates | CI pass (18/18 green) \| Conflicts pass \| Feedback pass |
| Areas changed | Same module scope — `agent_orchestration::workflow_runs`, `session_db::run_ledger`, `core::all`, `tests/json_rpc_e2e.rs` |
| Linked issues | #3375, #3370 |
| PR-Issue alignment | PR1 slice fully aligned with epic acceptance criteria for this increment. Start/stop/resume/UI remain deferred as documented. |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Continuation review after CodeRabbit's four CHANGES_REQUESTED items were resolved: diagnostic logging added to all validation flows, `has_cycle` comparison fixed to use `indegree.len()` (unique node count) with regression test, RPC handler success-exit logs added, u64→i64 offset cast made safe via `i64::try_from`. Stacked base #3497 merged 2026-06-08. |
| CodeRabbit dedup | All four CR findings confirmed resolved by CodeRabbit; CR moved to APPROVED. Skipped in review body. |
| Resolution actions | Verified logging coverage in `ops.rs` and `schemas.rs`, cycle detection fix, offset cast fix. |
| Surrounding code checked | `list_workflow_runs` SQL parameterization; `schemas.rs` correlation ID generation |
| Dependency audit | N/A — no new deps |
| Test coverage | CI coverage gate passed (diff-cover ≥ 80%). `duplicate_phase_names_do_not_report_false_cycle` regression test added for the Kahn's fix. |
| Impact scan | No existing callers affected. All changes are additive. |

#### Findings
- [minor] `src/openhuman/session_db/run_ledger/types.rs` — `WorkflowRunListRequest.status` is `Option<String>`, not `Option<WorkflowRunStatus>`. Invalid status values silently return empty results. Consistent with `AgentRunListRequest` pattern; worth tightening when the engine lands.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | APPROVE — CI 100% green, all prior CR issues resolved, code correct, no critical/major findings |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3500#pullrequestreview-4458116402 |
