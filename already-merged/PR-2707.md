# PR #2707 — feat(codegraph,skills): code-retrieval engine + agent tools + skill registry & skills_run (D1–D3) [draft]
- **Author**: @sanil-23
- **Branch**: feat/codegraph-skills → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2707
- **Status**: changes-requested
- **Last reviewed commit**: ee554498777d25f3a6324cc49a3f30ce972edbd2
- **Last review date**: 2026-05-27T15:07:40Z

## AI Summary

**What it does**: Adds a content-addressed code retrieval engine (`codegraph`) that indexes a git repo's blobs via BM25 lexical + structural-aug dense embeddings (RRF-fused), exposes it as two agent tools (`codegraph_index`, `codegraph_search`), introduces a skills registry (`SkillDefinition` wrapping `AgentDefinition` + declared `[[inputs]]`), and adds a `skills_run` RPC that runs the orchestrator agent focused on a given skill in the background. Also fixes a pre-existing test build break in `config/ops_tests.rs`.

**Breaking risk**: Zero — purely additive. No changes to existing public APIs, exported types, DB schemas, or config formats.

**Security risk**: Zero — no hardcoded secrets, no SQL injection (parameterized queries throughout), no command injection (git subprocess uses hardcoded args), no new external dependencies.

**Bottom line**: Safe to merge — once CI is green and WIP coverage gaps are addressed.

## Review History

### Review 1 — 2026-05-27T15:07:40Z
**Type**: Fresh
**Commit**: ee554498777d25f3a6324cc49a3f30ce972edbd2
**Summary**: New `codegraph` module (store + index + search), two agent tools registered in `all_tools_with_runtime`, a skills registry, `skills_run` RPC handler with background orchestrator execution and per-run log streaming, and a one-line test fix. 2358 lines added, no deletions.
**Gates**: CI FAILING (PR Submission Checklist fail; E2E macOS/Windows pending) | Conflicts: pass | Unresolved feedback: N/A (fresh)
**Areas changed**: Rust core (codegraph/, skills/, tools/, embeddings/)
**Red flags**: Draft PR with acknowledged WIP gaps (tool wrapper + handler tests uncovered; no `skill_run_status` endpoint)
**Linked issues**: None (tracked at sanil-23/openhuman#12)
**PR-Issue alignment**: N/A — no linked issue in this repo
**CodeRabbit dedup**: CodeRabbit review was skipped per CI output
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — all new files + one-line patch to existing test
**Dependency audit**: No new external dependencies introduced
**Test coverage**: 13 unit tests covering store, index, search, registry, run_log. Tool wrappers (codegraph/mod.rs) and `skills_run` handler (schemas.rs) explicitly noted as uncovered in PR checklist — flagged.
**Impact scan**: Additive only; `all_tools_with_runtime` gains two new tools (no signature changes to existing tools). `skills` schemas gain one new registered controller.
**Findings**:
- [minor] `src/openhuman/codegraph/index.rs`:214 + `src/openhuman/codegraph/search.rs`:50 — `l2_normalize` is duplicated identically in both files. Extract to a shared location.
- [minor] `src/openhuman/codegraph/index.rs`:583 — Developer home path `/home/sanil/vezures/...` hardcoded as default fallback in `#[ignore]`d `index_e2e_cloud` test.
- [minor] `src/openhuman/skills/registry.rs`:94 — No dedup between builtin and runtime skill IDs in `load_skills`; precedence is implicit (builtins win via first-match).
- [minor] `src/openhuman/skills/schemas.rs` handle_skills_run — No `skill_run_status`/cancel endpoint; background runs can only be tracked via log file path. PR body acknowledges this as follow-up.
**Action taken**: COMMENT — CI failing + pending checks; minor findings only; code otherwise clean
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2707#pullrequestreview-4373522392
