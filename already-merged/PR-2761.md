# PR #2761 — fix(config): reconcile orphaned provider references on upgrade (#2759)
- **Author**: @sanil-23
- **Branch**: fix/reconcile-orphaned-providers → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2761
- **Status**: clean
- **Last reviewed commit**: 62ad81b59a7be0ab53080ed71994dce02364fab3
- **Last review date**: 2026-05-27T15:06:43Z

## Review History

### Review 1 — 2026-05-27T15:06:43Z
**Type**: Fresh
**Commit**: 62ad81b59a7be0ab53080ed71994dce02364fab3
**Summary**: Adds a v5→v6 startup migration (`reconcile_orphaned_providers`) that resets per-workload provider strings referencing a removed cloud provider back to managed, and clears a dangling `primary_cloud`. Touches 4 files: `migrations/mod.rs` (wiring), `migrations/mod_tests.rs` (+1 integration test), and two new files for the migration module + its tests.
**Gates**: CI failing (Rust Core Coverage, Rust Tauri Coverage, Coverage Matrix Sync, PR Submission Checklist failing; many checks pending) | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (migrations)
**Red flags**: Pushed with `--no-verify` (pre-push hook bypassed due to pnpm/node not on non-interactive shell PATH; cargo check/test/fmt verified manually)
**Linked issues**: Closes #2759
**PR-Issue alignment**: All 3 acceptance criteria met — orphaned provider scrubbed to managed, regression tests cover all cases, diff coverage ≥ 80% (93% claimed, 64/64 lines per cargo-llvm-cov)
**CodeRabbit dedup**: CR flagged log-redaction issue (raw provider value logged verbatim) — already addressed by sanil-23; `redact_provider_for_log` helper added, slug kept, model segment masked. CR final state: APPROVED.
**Resolution actions**: M3gA-Mind left 4 issues (missing run_pending integration test, vestigial anyhow::Result, workload_fields exhaustiveness, trim asymmetry with factory) — all 4 confirmed addressed in follow-up commits. M3gA-Mind final state: "LGTM — all review items addressed. Ready to merge."
**Surrounding code patterns checked**: migrations/mod.rs (save/rollback pattern, version-gated branch structure)
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 11 unit tests in reconcile_orphaned_providers_tests.rs + 1 integration test in mod_tests.rs (`run_pending_v5_to_v6_reconciles_orphaned_providers`). Covers: orphan scrub, keep-when-present, temperature suffix, openhuman: normalize, sentinels/local untouched, bare string, all-9-workloads, dangling/valid primary_cloud, clean no-op, idempotency. ~93% changed-line coverage.
**Impact scan**: Pure in-memory config mutation; no exported API changes; no callers affected. Factory's fail-closed path intentionally unchanged.
**Findings**: None beyond what CodeRabbit and M3gA-Mind already caught and confirmed resolved.
**Action taken**: COMMENT — code is clean, CI not fully green. Will approve once CI passes.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2761#pullrequestreview-4373514031

## AI Summary
**What it does**: Adds a one-time idempotent startup migration that fixes orphaned provider references left in `Config` after a user removes a BYOK cloud provider. Per-workload `*_provider` strings whose slug no longer exists in `cloud_providers` are reset to `None` (managed), and a dangling `primary_cloud` id is cleared. Runs once on the v5→v6 schema bump, co-gated with the existing `repair_http_request_limits` migration.
**Breaking risk**: Zero — pure in-memory config mutation; no API, schema, or exported type changes. Migration is idempotent and best-effort (failures retried next launch).
**Security risk**: Zero — no network, no secrets touched. Log redaction is correctly implemented (model segment masked, slug retained for diagnostics).
**Bottom line**: Safe to merge — straightforward data migration with thorough tests and no side effects.
