# PR #2883 — fix(health): add legacy alias for health_snapshot (#2852)
- **Author**: @YOMXXX
- **Branch**: feature/health-snapshot-alias → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2883
- **Status**: changes-requested
- **Last reviewed commit**: f5008fac56e4c7ee3d1ab039ad6d0d66dfdd1305
- **Last review date**: 2026-05-29T00:00:00Z

## Review History

### Review 1 — 2026-05-29T00:00:00Z
**Type**: Fresh
**Commit**: f5008fac56e4c7ee3d1ab039ad6d0d66dfdd1305
**Summary**: Adds a legacy alias mapping `health_snapshot` → `openhuman.health_snapshot` on both the Rust server side (`src/core/legacy_aliases.rs`) and TypeScript frontend side (`app/src/services/rpcMethods.ts`). Fixes a Sentry-reported "unknown method" error on older clients. Closes #2852.
**Gates**: CI pass (2 checks: CodeRabbit pass, contributor invite skip) | No conflicts | CodeRabbit has active CHANGES_REQUESTED
**Areas changed**: Rust core (legacy_aliases.rs), Frontend RPC methods (rpcMethods.ts)
**Red flags**: Unquoted object key in TypeScript will break the Rust parity test parser
**Linked issues**: #2852
**PR-Issue alignment**: Meets all acceptance criteria structurally — alias added, unit test added — but the TypeScript key formatting bug breaks the parity test that is itself part of the acceptance criteria.
**CodeRabbit dedup**: CodeRabbit flagged the unquoted `health_snapshot` key in rpcMethods.ts at line 44 as critical — `parse_frontend_legacy_aliases` calls `quoted_value()` on the key side, which panics when no quote character is present (confirmed in legacy_aliases.rs:170-181). All findings covered by CodeRabbit.
**Resolution actions**: None
**Surrounding code patterns checked**: `src/core/legacy_aliases.rs` — `quoted_value()` and `parse_frontend_legacy_aliases()` functions read directly. Every other key in LEGACY_METHOD_ALIASES uses quoted strings (single quotes for dot-containing keys). The `health_snapshot` key is the sole unquoted entry.
**Dependency audit**: N/A — no new dependencies
**Test coverage**: Rust unit test `health_snapshot_resolves_to_canonical` added. The `frontend_legacy_aliases_match_server_alias_table` parity test would fail on the unquoted key.
**Impact scan**: Purely additive — new alias entry only, no existing callers affected.

**Findings**:
- [major] app/src/services/rpcMethods.ts:44 — `health_snapshot` key is unquoted; `parse_frontend_legacy_aliases()` calls `quoted_value()` on the key side and panics when no quote is found. Fix: `'health_snapshot': CORE_RPC_METHODS.healthSnapshot`. **COVERED BY CODERABBIT — not posted.**

**Action taken**: No GitHub review posted — all findings duplicated by CodeRabbit (active CHANGES_REQUESTED). Tracking file updated.
**GitHub review URL**: N/A

## AI Summary
- **What it does**: Adds a string alias so older clients calling `health_snapshot` (without the `openhuman.` prefix) are transparently remapped to `openhuman.health_snapshot` by the dispatch layer. Fixes one Sentry-reported crash on Linux Oracle.
- **Breaking risk**: Zero — purely additive; no existing callers or aliases modified.
- **Security risk**: Zero — static string mapping, no auth surface, no injection vector.
- **Bottom line**: Safe to merge once the unquoted key bug is fixed.
