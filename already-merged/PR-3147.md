# PR #3147 — test(auth): expect dropped OAuth profile missing access_token (fixes red Rust E2E on main)
- **Author**: @sanil-23
- **Branch**: fix/auth-profile-missing-token-test → main
- **Created**: 2026-06-01
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3147
- **Status**: clean
- **Last reviewed commit**: 1765410e68d56ac4cad63e045ecef27c0b5003bd
- **Last review date**: 2026-06-01T12:27:08Z

## AI Summary
- **What it does**: Updates one e2e test assertion in `tests/config_auth_app_state_connectivity_e2e.rs` to match the behavior shipped in #3125. That PR changed `AuthProfilesStore::load` to drop OAuth profiles missing `access_token` rather than return an error — but didn't update the test, leaving the `Rust E2E (mock backend)` required check red on main and blocking all other PRs. This PR fixes the test to assert the new contract: load succeeds, dropped profile is absent from the map, active profile pointer is purged, and the persisted JSON is cleaned up.
- **Breaking risk**: Zero — test-only change, no production code touched.
- **Security risk**: Zero.
- **Bottom line**: Safe to merge.

## UI Impact
N/A — test-only change, no frontend code touched.

## Review History

### Review 1 — 2026-06-01T12:27:08Z
**Type**: Fresh
**Commit**: 1765410e68d56ac4cad63e045ecef27c0b5003bd
**Summary**: Single-file test-only change to `tests/config_auth_app_state_connectivity_e2e.rs`. Replaces `expect_err` + error-string assertion with `expect` (success) + three assertions covering the new drop contract from #3125: profile absent from map, active pointer purged, persisted JSON cleaned up.
**Gates**: CI partially failing (Playwright lane 1, Frontend Coverage, Rust Core Coverage — pre-existing on main, unrelated to this Rust test change); Rust E2E (mock backend) now passing; no conflicts
**Areas changed**: Tests (Rust e2e)
**Red flags**: None
**Linked issues**: None (follow-up to #3125; no tracking issue)
**PR-Issue alignment**: N/A — no linked issue
**CodeRabbit dedup**: CodeRabbit left no inline comments; approved. Nothing to skip.
**Resolution actions**: None
**Surrounding code patterns checked**: Reviewed existing bad-kind drop assertions in same test function as reference pattern
**Dependency audit**: N/A — test-only
**Test coverage**: N/A — the changed lines are the test assertions themselves; covers happy path (drop + persist) and mirrors existing bad-kind pattern
**Impact scan**: N/A — no exported functions/types changed
**Findings**: None
**Action taken**: COMMENT — code is clean, CI has pre-existing unrelated failures; will approve once full suite is green
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3147#pullrequestreview-4401145263
