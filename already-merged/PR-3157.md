# PR #3157 — feat(ui): hide dev-only features in production, clean up homepage

## Metadata
| Field | Value |
|-------|-------|
| Author | @senamakel |
| Branch | fix/prod-build-fixes → main |
| Created | 2026-06-01 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3157 |
| Status | clean |
| Last reviewed commit | ed63eee511b8184b7c5766acc5e1839d94788050 |
| Last review date | 2026-06-01T15:31:32Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Gates dev-only UI features (Intelligence tabs: tasks/workflows/council, Memory sub-tabs, Runners tab, MCP Servers, meeting button) behind the existing `IS_DEV` flag so production builds show a cleaner, focused UI. Also removes the stale EarlyBirdy promo banner and duplicate Routines card from the homepage. |
| Breaking risk | Zero | Visibility-only changes. No public APIs, shared types, DB schemas, config formats, or exported functions changed. |
| Security risk | Zero | No authentication, authorization, secrets, or injection surfaces touched. |
| Bottom line | Safe to merge | Pure conditional rendering gates using an established pattern; no logic changes. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | PR description contains AI-generated metadata template sections, but the code changes themselves are concise, correct, and appropriately minimal. |
| Structural signals | None | No over-abstraction, no unnecessary wrappers. Tab filtering via `devOnly` property on config objects is idiomatic. |
| Content signals | None | Variable names are clear (`allSubTabs`, `allTabs`). No placeholder TODOs or dead code. |
| Behavioral signals | None | PR is well-scoped, test cleanup is correct, changes match the stated purpose. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | Production users see: no Routines card or EarlyBirdy banner on Home; Intelligence page defaults to Memory tab with only Memory and Subconscious tabs visible; MemorySection shows only Memory Tree sub-tab; Skills page has no Runners tab and no "Other" category; MCP Servers shows "Coming Soon" placeholder; Human page has no "Send to meeting" button. |
| User flow | Modified — dev-only features gated; production users have fewer navigation options by design |
| Affected surfaces | Home, Intelligence page, MemorySection component, HumanPage, Skills page |
| Responsiveness | Not applicable — no layout structure changes |
| Accessibility | Not applicable — no new interactive elements introduced |
| i18n | `misc.comingSoon` and `channels.mcp.description` keys used in new "Coming Soon" placeholder; existing keys from prior usage |
| Performance | Slightly improved — fewer components rendered in production |
| Edge states | MemorySection in production renders PillTabBar with a single pill (memoryTree only) — flagged as minor in review, needs visual smoke check |
| Risk | Low |
| Recommendation | Needs quick visual smoke check in prod build for single-pill PillTabBar in MemorySection |

## Review History

### Review 1 — 2026-06-01T15:31:32Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | ed63eee511b8184b7c5766acc5e1839d94788050 |
| Gates | CI fail (PR Submission Checklist failing; Rust Coverage, Frontend Coverage, Playwright pending) \| Conflicts pass \| Feedback pass |
| Areas changed | Frontend (React pages, components) |
| Linked issues | None |
| PR-Issue alignment | N/A — no linked issue; scope is well-defined by PR description |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds `devOnly` property to tab config objects across Intelligence, MemorySection, and Skills pages, then filters them using the existing `IS_DEV` flag. Removes EarlyBirdy banner and Routines card from Home.tsx entirely. Test suite updated to remove now-irrelevant mocks and deleted EarlyBirdy banner tests. |
| CodeRabbit dedup | CodeRabbit review still in progress (pending) at time of review — no findings to dedup |
| Resolution actions | None — no prior graycyrus review |
| Surrounding code checked | None needed — IS_DEV pattern is self-evident from the diff |
| Dependency audit | N/A — no new dependencies |
| Test coverage | EarlyBirdy banner tests removed (correct — feature deleted). Remaining Home tests updated and passing (17/17 per PR author). No new logic branches to cover (conditional rendering gates on existing components). |
| Impact scan | No exported functions or shared types changed. IS_DEV is a static import from config.ts — no runtime side effects. |

#### Findings
- [minor] `app/src/components/intelligence/MemorySection.tsx:53` — In production, `subTabs` has exactly one entry (`memoryTree`). `PillTabBar` rendering a single pill may look visually awkward depending on its styling. Recommend a quick smoke check in a prod build; suppress the tab bar if it looks off.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — code is clean, CI is failing (PR Submission Checklist) and several jobs pending; will approve once CI is green |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3157#pullrequestreview-4402517745 |
