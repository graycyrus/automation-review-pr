# PR #2878 — perf(learning/user_profile): Aho-Corasick DFA for preference extraction + pattern coverage
- **Author**: @mysma-9403
- **Branch**: feature/aho-corasick-user-profile → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2878
- **Status**: clean
- **Last reviewed commit**: 8c486b4eab645f66b182000c573ba282208b63ce
- **Last review date**: 2026-05-29T00:00:00Z

## Review History

### Review 1 — 2026-05-29T00:00:00Z
**Type**: Fresh
**Commit**: 8c486b4eab645f66b182000c573ba282208b63ce
**Summary**: Replaces the O(N×M) `str::contains` per-pattern preference scan in `UserProfileHook` with a single Aho-Corasick DFA pass per sentence. Eliminates per-turn `String` allocations from re-lowercasing, adds word-boundary validation to prevent false positives on word continuations (e.g. "I preferred"), expands patterns from 20 to 28, extends sentence delimiters to include '?' and ';', and backs everything with 15 unit tests (up from 5).
**Gates**: CI PENDING — multiple checks still running (Rust Core Tests, coverage, E2E Appium suites, Frontend Unit Tests). Rust Quality, TypeCheck TS, Build Tauri App, Frontend Coverage, Rust Tauri Coverage all pass.
**Areas changed**: Rust core (`src/openhuman/learning/user_profile.rs`), Cargo.toml/Cargo.lock
**Red flags**: None
**Linked issues**: References #2842, #2870 as prior art for same pattern
**PR-Issue alignment**: N/A (performance/correctness improvement, no linked issue required)
**CodeRabbit dedup**: CodeRabbit review was skipped — no findings to dedup
**Resolution actions**: None
**Surrounding code patterns checked**: Reviewed full diff; checked pattern logic, word-boundary implementation, LazyLock initialization, MatchKind semantics
**Dependency audit**: `aho-corasick = "1.1"` — BurntSushi (Andrew Gallant), MIT/Unlicense dual-licensed, actively maintained, already a transitive dep via `regex`. Direct dep declaration is the right call. No supply-chain concerns.
**Test coverage**: Excellent — 15 tests covering happy path, negative path, cap enforcement, word-boundary correctness (alphanumeric reject + non-alphanumeric accept), bare-pattern empty-tail rejection, expanded delimiters (?/;), expanded pattern coverage, Unicode safety (Cyrillic/Polish/emoji), and DFA construction smoke test.
**Impact scan**: Internal module, no exported API changes, no cross-cutting impact.
**Findings**: None
**Action taken**: COMMENT — CI pending; code is clean. Will approve once CI is green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2878#pullrequestreview-4385335591

## AI Summary
**What it does**: Swaps the O(N×M) substring-scan loop in `UserProfileHook::extract_preferences` for a single Aho-Corasick DFA pass with proper word-boundary enforcement, while also expanding the pattern set (20→28) and sentence delimiter set to catch more real preference statements. Runs on every user turn so the allocation savings are real.
**Breaking risk**: Zero — private internal module, no exported types or public API changes.
**Security risk**: Zero — pure string parsing, no I/O, no injection surfaces, no secrets.
**Bottom line**: Safe to merge.
