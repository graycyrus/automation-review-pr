# PR #2936 — feat(vault): expose approved markdown writes
- **Author**: @Zavianx
- **Branch**: codex/OH-2908-vault-write-state → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2936
- **Status**: clean
- **Last reviewed commit**: f67e8955a4d17fa190b07423402be89f5cce8745
- **Last review date**: 2026-05-29T11:19:57Z

## Review History

### Review 1 — 2026-05-29T11:19:57Z
**Type**: Fresh
**Commit**: f67e8955a4d17fa190b07423402be89f5cce8745
**Summary**: Adds `write_state`/`write_state_reason` fields to user-added vaults, exposes a new `vault.write_markdown` RPC endpoint, and registers a `VaultWriteMarkdownTool` agent tool. VaultPanel now displays writable/read-only/unavailable badges with reason text. Path safety guards: absolute path rejection, `..` traversal blocking, non-markdown extension rejection, symlink target/ancestor escape detection.
**Gates**: CI FAILING (i18n Coverage fail, multiple checks pending) | Conflicts pass | No prior feedback
**Areas changed**: Rust core (vault/ops, vault/store, vault/types, vault/schemas, tools/filesystem), Frontend (VaultPanel, vault.ts), i18n (en.ts), Docs (coverage matrix, architecture)
**Red flags**: i18n Coverage CI failure
**Linked issues**: Closes #2908
**PR-Issue alignment**: All 6 acceptance criteria addressed. Write state surfaced in UI; approved write path implemented; silent dead-ends replaced with explicit reasons; permission safe (approval flag + security gate + path validation); regression safety via comprehensive test suite; coverage gate pending CI.
**CodeRabbit dedup**: CodeRabbit pending — no findings to dedup yet
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — implementation follows existing vault ops patterns
**Dependency audit**: No new external dependencies introduced
**Test coverage**: Strong — vault_write_markdown_requires_explicit_approval, vault_write_markdown_creates_and_updates_relative_markdown, vault_write_markdown_rejects_escape_paths_and_non_markdown, vault_write_markdown_rejects_symlink_escape (Rust); VaultPanel write state display tests (Vitest); vault.ts command dispatch tests (Vitest)
**Impact scan**: CoreVault type extended with new optional fields (serde default annotations prevent breakage). vault_write_markdown registered under file_write preference group — no new tool filter groups needed.
**Findings**:
- [minor] src/openhuman/vault/store.rs — `vault_write_state_for_root_path` uses `metadata.permissions().readonly()` which on Unix only checks whether all write bits are zero, not whether the current process user can write. A root-owned `755` dir reports Writable but the agent can't write there. Safe failure but misleading UI state.
- [minor] src/openhuman/vault/ops.rs — TOCTOU between `symlink_metadata(&target)` check and `fs::write(&target, content)`. Inherent POSIX limitation; risk is minimal given the write requires user approval on a user-controlled vault directory.

## AI Summary
**What it does**: Adds an explicit vault write capability surface: user-added vaults now derive and display their write state (writable/read-only/unavailable) at list/get/create time instead of leaving it ambiguous, and a new approved write path (`vault.write_markdown`) lets agents create or update `.md`/`.markdown` files inside registered writable vaults with layered path safety checks.
**Breaking risk**: Low. CoreVault struct gains two fields with `#[serde(default)]` — existing serialized data deserializes without migration. All existing vault RPC paths are unchanged.
**Security risk**: Low. The write path requires `approved: true`, checks write state, validates relative markdown-only paths, blocks `..` traversal, rejects symlink writes, and canonicalizes ancestors. TOCTOU noted but low-risk.
**Bottom line**: Safe to merge — once CI is green.

**Action taken**: COMMENT posted (CI failing: i18n Coverage + multiple pending checks; 0 critical, 0 major, 2 minor)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2936#pullrequestreview-4389039283
