# PR #3134 — Add deployment workflow for OpenHuman project
- **Author**: @manucian-official
- **Branch**: (feature branch) → main
- **Created**: 2026-06-01
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3134
- **Status**: changes-requested
- **Last reviewed commit**: 2c800fddca3a14dce9b76aad6f529cc58ecdeddd
- **Last review date**: 2026-06-02T00:00:00Z

## AI Summary
- **What it does**: Adds a multi-stage GitHub Actions deployment workflow (`deploy.yml`) that builds Rust core binaries and Tauri desktop bundles across macOS/Linux/Windows, pushes a Docker image to GHCR, and publishes GitHub releases. Triggered manually via `workflow_dispatch` with environment (staging/production) and platform selection inputs.
- **Breaking risk**: Zero — pure addition of a new workflow file, no changes to existing code or APIs.
- **Security risk**: Low — secrets properly referenced via `${{ secrets.* }}`, GITHUB_TOKEN permissions scoped to `contents: write` + `packages: write`, no injection surfaces in user-controlled inputs.
- **Bottom line**: Safe to merge as an additive change, but several workflow logic issues should be fixed before using it in production deployments.

## UI Impact
N/A — CI/CD workflow only, no frontend code changed.

## Review History

### Review 1 — 2026-06-01T11:55:18Z
**Type**: Fresh
**Commit**: 1b1af2cea0c9f3d5bf2f0beba3ef952136cb1644
**Summary**: PR adds `.github/deploy.yml`, a 578-line manual-dispatch deployment workflow covering Rust core + Tauri desktop builds for 5 platforms, Docker image push to GHCR, and GitHub release publishing. Single file change, pure addition.
**Gates**: CI FAILING (E2E lane 1/4, Frontend Coverage, Rust Core Coverage, Rust E2E failing — unrelated to this workflow file) | Conflicts: N/A | Unresolved feedback: N/A
**Areas changed**: CI/CD workflow
**Red flags**: CI failing; macOS notarization is a no-op placeholder
**Linked issues**: None
**PR-Issue alignment**: No linked issue; acceptable for CI/CD workflow additions
**CodeRabbit dedup**:
- `.app` pattern in checksum find command won't match macOS bundles (directories) — SKIP, covered
- `actions/create-release@v1` deprecated, use `softprops/action-gh-release@v3` — SKIP, covered
**Resolution actions**: None
**Surrounding code patterns checked**: None (no existing deploy workflows to compare against)
**Dependency audit**: N/A — no package dependencies changed
**Test coverage**: N/A — workflow file, no unit tests applicable
**Impact scan**: N/A — additive CI file
**Findings**:
- [major] `.github/deploy.yml:167` — `build-rust-core`, `build-desktop`, `build-docker` all `needs: [validate]` only; `pre-checks` job is never a prerequisite for any build. Typecheck and lint run in parallel with builds, not before them.
- [major] `.github/deploy.yml:155,159` — `continue-on-error: true` on both typecheck and lint steps makes them advisory. Failing checks won't surface as workflow failures even if `pre-checks` were properly gated.
- [major] `.github/deploy.yml:396-419` — macOS notarization step is an explicit placeholder: validates env vars then exits. Production macOS bundles won't be notarized and will be quarantined by Gatekeeper on modern macOS.
- [minor] `.github/deploy.yml:484` — `publish-release` uses `if: always() && needs.validate.result == 'success'`; if build jobs fail, the release still publishes with incomplete artifact sets.
**Action taken**: COMMENT (CI failing + code has issues)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3134#pullrequestreview-4400925662

### Review 2 — 2026-06-02T00:00:00Z

#### Context
| Field | Value |
|-------|-------|
| Type | Continuation |
| Commit | 2c800fddca3a14dce9b76aad6f529cc58ecdeddd |
| Gates | CI pending \| Conflicts: none (merge commit applied) \| Feedback: unresolved (threads closed without code changes) |
| Areas changed | Merge of upstream/main into PR branch — no author code changes |
| Linked issues | None |
| PR-Issue alignment | N/A |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | New commit is a merge of upstream/main into the PR branch by senamakel. The deploy.yml file is unchanged from Review 1. All three major findings from Review 1 remain unaddressed. Review threads were marked resolved by the author without corresponding code fixes. |
| CodeRabbit dedup | No new CodeRabbit findings |
| Resolution actions | None — graycyrus threads were resolved by author without code changes; did not re-resolve |
| Surrounding code checked | None |
| Dependency audit | N/A |
| Test coverage | N/A |
| Impact scan | N/A |

#### Findings
- [major] `.github/deploy.yml:149,153` — `continue-on-error: true` still present on typecheck and lint — still advisory, won't block deployments
- [major] `.github/deploy.yml:167` — `build-rust-core`/`build-desktop`/`build-docker` still only `needs: [validate]` — pre-checks still not gating builds
- [major] `.github/deploy.yml:396-419` — macOS notarization still a placeholder

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — flagged that review threads were resolved without code fixes; CI still pending |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3134#pullrequestreview-4403976404
