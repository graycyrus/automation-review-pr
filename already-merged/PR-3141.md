# PR #3141 — fix(routines): deduplicate morning_briefing jobs on seed boot (#3126)
- **Author**: @YellowSnnowmann
- **Branch**: fix/3126-morning-briefing-duplicate → main
- **Created**: 2026-06-01
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3141
- **Status**: clean
- **Last reviewed commit**: 1185fa2ea69814ccb308f5c9a0735b7d9aacef39
- **Last review date**: 2026-06-01T11:53:47Z

## AI Summary
- **What it does**: Adds `dedup_named_jobs()` to the cron store, called best-effort at the top of `seed_proactive_agents`, to collapse duplicate named cron jobs left over from the old non-atomic check-then-insert pattern. Keeps the row with the most run history (ties broken by earliest `created_at`, then `id ASC`), deletes the rest. Fully idempotent.
- **Breaking risk**: Zero. Internal boot-time cleanup; no public API changes, no shared types modified.
- **Security risk**: Zero. No user-facing input, no new network surface, pure DB maintenance.
- **Bottom line**: Safe to merge — narrowly scoped bug fix with good test coverage.

## UI Impact
- **Visual change**: Users with the double Morning Briefing card will see exactly one card after upgrading.
- **User flow**: Unchanged — routine list display, scheduling, and history are unaffected.
- **Affected surfaces**: Routines screen (indirect; the duplicate card disappears).
- **Risk**: None
- **Recommendation**: Approve as-is

## Review History

### Review 1 — 2026-06-01T11:53:47Z
**Type**: Fresh
**Commit**: 1185fa2ea69814ccb308f5c9a0735b7d9aacef39
**Summary**: Adds `dedup_named_jobs` to `src/openhuman/cron/store.rs`, re-exports it via `cron/mod.rs`, and calls it at the top of `seed_proactive_agents` in `seed.rs`. Five unit tests in `store_tests.rs` cover the no-op, dedup-by-history, tie-break, and unnamed-jobs-ignored paths.
**Gates**: CI pending (E2E lanes + coverage jobs) | Conflicts pass | No prior graycyrus feedback
**Areas changed**: Rust core (cron module)
**Red flags**: None
**Linked issues**: #3126
**PR-Issue alignment**: Fully meets all 6 acceptance criteria — no duplicate card, idempotent seeding, existing duplicates cleaned up, run history preserved, regression tests present, coverage gate addressed.
**CodeRabbit dedup**: One finding (non-deterministic ORDER BY) was already addressed in commit 1185fa2 with `j.id ASC`. Nothing else to skip.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: `store.rs` existing logging pattern (log::info! consistent at line 196), FK schema confirming `ON DELETE CASCADE` on `cron_runs(job_id)` at line 599.
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 5 unit tests, good edge case coverage. Minor: `dedup_named_jobs_no_op_when_no_duplicates` uses unnamed jobs rather than named-but-unique jobs; slightly misleading test name but correct behavior.
**Impact scan**: `dedup_named_jobs` is only called from `seed.rs`. No other callers. Export via `mod.rs` is additive and doesn't break existing consumers.
**Findings**:
- [minor] `store.rs` — `conn.prepare()` for the canonical ID query is called inside the per-name loop; should be hoisted before the loop
- [minor] `store_tests.rs` — `dedup_named_jobs_no_op_when_no_duplicates` tests unnamed jobs (not named-but-unique); test name is misleading / overlaps with `dedup_named_jobs_ignores_unnamed_jobs`
**Action taken**: COMMENT — code is clean, CI pending; noted minor issues, flagged will approve once CI green
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3141#pullrequestreview-4400916473
