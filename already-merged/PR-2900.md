# PR #2900 — feat(mascot): add emotional reaction layer for conversation state
- **Author**: @staimoorulhassan
- **Branch**: feat/mascot-emotional-reactions → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2900
- **Status**: approved
- **Last reviewed commit**: 754e978ddd653bb85d12c190ae3d71970582fadc
- **Last review date**: 2026-05-29T06:49:18Z

## Review History

### Review 1 — 2026-05-29T06:49:18Z
**Type**: Fresh
**Commit**: 754e978ddd653bb85d12c190ae3d71970582fadc
**Summary**: Adds three new `MascotFace` variants (`curious`, `proud`, `cautious`) to Ghosty and RiveMascot, extends `useHumanMascot.ts` with emoji sets, text regexes, and per-turn work-tracking refs, and wires `onSubagentDone` into the state machine so successful tool/subagent turns promote the ack face to `proud`. Closes #1144.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Frontend (Mascot component, useHumanMascot hook), Tests
**Red flags**: PR description mentions `--no-verify` on push (env issue — CI cargo fmt passes cleanly)
**Linked issues**: #1144
**PR-Issue alignment**: All acceptance criteria from #1144 met — new emotional states, meaning-driven reactions, tool/subagent outcome wiring, fallback behavior, reuse of existing chat event surface, test coverage ≥ 80% (CI gate passes), code comments document signal→emotion mapping. Documentation checkbox in issue is satisfied via code comments (no separate doc file required).
**CodeRabbit dedup**: CodeRabbit approved, no inline comments posted — nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: SubMascotLayer.tsx (switch on ToolTimelineEntryStatus, not MascotFace — safe), CustomGifMascot.tsx (accepts MascotFace prop — additive, safe), MascotWindowApp.tsx (useState<MascotFace> — safe)
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 11 new tests in useHumanMascot.test.ts covering new emotions, proud promotion, subagent reactions, turn reset. Coverage Gate CI check: pass.
**Impact scan**: `MascotFace` union extended additively. All exhaustive consumers in diff updated (FACE_PRESETS, restMouthPath, FACE_TO_POSE). No external exhaustive switches found — SubMascotLayer switches on ToolTimelineEntryStatus, not MascotFace.
**Findings**:
- [minor] `CAUTIOUS_TEXT_RE` and `CURIOUS_TEXT_RE` include very common phrases ("make sure", "actually", "turns out") — likely over-triggering in routine responses. Design tuning concern, not a correctness bug.
- [minor] `proud`/`cautious` both map to `idle` in RiveMascot — Rive users see no distinction. Documented in code comment; acceptable placeholder pending dedicated Rive animations.
- [minor] `blushOpacity: 0.8` on `curious` reads more like excitement than curiosity visually. Design call.
**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2900#pullrequestreview-4387280704

## AI Summary
- **What it does**: Extends the mascot's emotional vocabulary with `curious`, `proud`, and `cautious` states, driven by conversation meaning (emoji signals, text regexes, and tool/subagent success tracking). `proud` is auto-promoted when a turn involved real tool or subagent execution. `⚠️` is reclassified from `concerned` to `cautious`.
- **Breaking risk**: Low. `MascotFace` union is extended additively; all existing consumers either have a `default` branch or are Records that now explicitly include the new variants.
- **Security risk**: Zero. Pure UI/animation logic — no auth, no network, no user data.
- **Bottom line**: Safe to merge.
