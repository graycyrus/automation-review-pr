# PR #2616 — fix: stale lastError on reconnect and short first chat bubble
- **Author**: @shriii257
- **Branch**: head → main
- **Created**: 2026-05-25
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2616
- **Status**: changes-requested
- **Last reviewed commit**: b6a9498a166453195a658670b0ba026991825033
- **Last review date**: 2026-05-29T03:51:39Z

## Review History

### Review 1 — 2026-05-29T03:51:39Z
**Type**: Fresh
**Commit**: b6a9498a166453195a658670b0ba026991825033
**Summary**: Single-file change to `connectivitySlice.ts` extending the `setBackend` error-clearing guard to cover `'connecting'` state in addition to `'connected'`. PR description claims two fixes (connectivitySlice + messageSegmentation) but only one is in the diff — no messageSegmentation code exists anywhere in this PR.
**Gates**: CI FAILING (PR Submission Checklist, E2E Linux Appium Chromium, Rust Core Tests Windows) | Conflicts: pass
**Areas changed**: Frontend (Redux slice)
**Red flags**: PR title and description describe two fixes; diff only contains one. messageSegmentation fix entirely absent from the code.
**Linked issues**: None (no `Closes #NNN` in description)
**PR-Issue alignment**: N/A — no linked issues
**CodeRabbit dedup**: CodeRabbit APPROVED with no inline comments. Nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: Read full `connectivitySlice.ts` to verify guard pattern consistency with `setInternet` and `setCore`.
**Dependency audit**: N/A — no dependency changes
**Test coverage**: Existing tests pass per CI (Frontend Unit Tests: pass). New test for messageSegmentation edge case mentioned in description but no such test or code exists in diff.
**Impact scan**: `setBackend` is called by `socketService` lifecycle callbacks. Change is purely additive (guard extended, not behavior removed). No callers break.
**Findings**:
- [major] connectivitySlice.ts — PR description and title claim two fixes (connectivitySlice + messageSegmentation). Only connectivitySlice is in the diff. Either the commit is incomplete or the description is stale/incorrect.
- [minor] CI: PR Submission Checklist fails because checklist checkboxes are not filled out in the PR template.

## AI Summary
- **What it does**: Extends the `setBackend` Redux reducer's error-clearing guard to also fire when transitioning to `'connecting'` (in addition to `'connected'`), preventing a stale disconnect error string from persisting in Redux state during the reconnect window.
- **Breaking risk**: Zero — no exported API, type, or state shape change. Pure behavioral fix inside an existing reducer branch.
- **Security risk**: Zero — no auth, no PII, no injection surface.
- **Bottom line**: Safe to merge (the code that exists is correct) — but CI must be green and the description mismatch must be resolved first.

**Action taken**: COMMENT — CI failing + major description discrepancy. Flagged missing messageSegmentation implementation and CI failures. Asked author to resolve before re-review.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2616#pullrequestreview-4386468527
