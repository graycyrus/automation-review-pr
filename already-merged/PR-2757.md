# PR #2757 — fix(tauri): verify openhuman:// registry registration on Windows (#2699)
- **Author**: @CodeGhost21
- **Branch**: fix/2699-windows-deeplink-registration-verify → main
- **Created**: 2026-05-27
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2757
- **Status**: approved
- **Last reviewed commit**: c8dd8bb63b8b41d50a43834c08ac0ef25839ed4f
- **Last review date**: 2026-05-27T15:07:11Z

## Review History

### Review 1 — 2026-05-27T15:07:11Z
**Type**: Fresh
**Commit**: c8dd8bb63b8b41d50a43834c08ac0ef25839ed4f
**Summary**: Adds `deep_link_registration_check.rs` — a new cross-platform module that reads back `HKCU\Software\Classes\openhuman\shell\open\command` after `tauri-plugin-deep-link::register_all()` returns on Windows and classifies the result into five states (Valid / Stale / MissingCommand / NotRegistered / ReadError). Upgrades the silent `log::warn!` to `log::error!` with redacted path details when the registration is unhealthy, and adds a PowerShell manual-repair guide to `troubleshooting-sign-in.md`.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Tauri shell (app/src-tauri/), docs (gitbooks/, CLAUDE.md)
**Red flags**: None
**Linked issues**: Closes #2699
**PR-Issue alignment**: Issue reports OAuth callbacks not reaching the app on Windows; PR correctly identifies the root cause (silent registry write failure / stale path after install move) and adds the diagnostic to make it Sentry-visible. Acceptance criteria met.
**CodeRabbit dedup**: CodeRabbit flagged path leaking in logs (using `{status:?}` for `Stale` variant). Already addressed in commit c8dd8bb via `status.redacted()` — skipped entirely.
**Resolution actions**: CodeRabbit CHANGES_REQUESTED → APPROVED after fix in c8dd8bb. No human reviewer threads.
**Surrounding code patterns checked**: lib.rs setup block (OwnedMutex pattern for HKEY RAII), existing deep_link_ipc_windows.rs module structure.
**Dependency audit**: No new crates. Adds `Win32_System_Registry` feature to existing `windows-sys = 0.59` — the crate is already in the tree, this is a feature flag only. No supply-chain risk.
**Test coverage**: 10 unit tests in `deep_link_registration_check::tests` covering token extraction (6 cases incl. malformed quoting), path comparison, command-vs-exe matching, `is_healthy` discriminator, and PII-leak redaction. Windows-only `verify_protocol_registration()` body covered by CI Windows runner (passed). Coverage Gate (diff-cover ≥ 80%) passed.
**Impact scan**: New module, no exported API changes. `lib.rs` setup block change is Windows-only and startup-only. No callers to audit.
**Findings**: None
**Action taken**: APPROVE
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2757#pullrequestreview-4373518181

## AI Summary
- **What it does**: Adds read-back verification of the `openhuman://` URL scheme registry entry on Windows at startup. If the entry is missing, stale (points at old exe), or unreadable, emits `log::error!` with redacted details and points to the new troubleshooting doc. Fixes the silent diagnostic gap behind issue #2699.
- **Breaking risk**: Zero — no API changes, no shared type changes, no config schema changes. Windows-only code path at startup.
- **Security risk**: Zero — read-only HKCU access, filesystem paths redacted to basenames before logging, no user-facing behavior change.
- **Bottom line**: Safe to merge.
