# PR #2884 — fix(cron): accept bare cron string in Schedule deserialization (#2866)
- **Author**: @YOMXXX
- **Branch**: feature → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2884
- **Status**: approved
- **Last reviewed commit**: d182e24d1dc8e28b1e5330bdc1366d3b9fc591b0
- **Last review date**: 2026-05-29T01:50:57Z

## Review History

### Review 1 — 2026-05-29T01:50:57Z
**Type**: Fresh
**Commit**: d182e24d1dc8e28b1e5330bdc1366d3b9fc591b0
**Summary**: Single-file change to `src/openhuman/cron/types.rs`. Drops derived `Deserialize` on `Schedule` and replaces it with a custom impl that first checks for a bare string (wraps as `Schedule::Cron` with defaults) and falls back to the existing internally-tagged enum format. Adds 3 tests covering bare string, tagged object, and roundtrip serialization.
**Gates**: CI pass | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (`src/openhuman/cron/types.rs`)
**Red flags**: None
**Linked issues**: #2866 (Cron update fails: Schedule field rejects bare cron strings)
**PR-Issue alignment**: All three acceptance criteria met — bare string deserialization works, regression tests present, diff coverage solid.
**CodeRabbit dedup**: CR flagged ScheduleTagged enum duplication / JSON coupling (nitpick). Skipped — covered. Focused on validation gap instead.
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — single-file change, self-contained.
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 3 new unit tests. schedule_deserializes_bare_cron_string, schedule_deserializes_tagged_cron_object, schedule_bare_string_roundtrip_serializes_as_tagged. All acceptance criteria paths covered.
**Impact scan**: Serialization is unchanged (always outputs tagged format). Callers using tagged input unaffected. Purely additive.
**Findings**:
- [minor] `src/openhuman/cron/types.rs:88` — Bare string path accepts any string without cron syntax validation. Pre-existing issue (expr: String is unvalidated throughout), but LLM callers increase exposure. Error surfaces at execution time, not deserialization. Not blocking.

## AI Summary
- **What it does**: Adds a custom `Deserialize` impl for `Schedule` so bare cron strings like `"0 9 * * 1"` are accepted in addition to the existing tagged object format `{"kind": "cron", "expr": "..."}`. Fixes a production bug (two Sentry events) where LLM tool calls and API callers were sending bare strings and getting deserialization errors.
- **Breaking risk**: Zero. Serialization path is unchanged. Existing callers using tagged format still work. Purely additive.
- **Security risk**: Zero. No auth, secrets, injection surfaces. The bare string is stored as a cron expression value.
- **Bottom line**: Safe to merge.

**Action taken**: APPROVED
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2884#pullrequestreview-4386043498
