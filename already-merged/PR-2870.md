# PR #2870 — perf(routing/quality): batch refusal detection via Aho-Corasick DFA
- **Author**: @mysma-9403
- **Branch**: (head) → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2870
- **Status**: clean
- **Last reviewed commit**: e9be8c375bf53ed254987349f2b2e5909515a7d9
- **Last review date**: 2026-05-29T00:00:00Z

## AI Summary
- **What it does**: Replaces `to_lowercase()` + 10 `starts_with` refusal checks with a single Aho-Corasick DFA pass over the first 200 bytes of trimmed responses. Expands phrase coverage from 10 to ~35 entries, adds empty-noise token detection for single-utterance fillers that clear the length floor, and eliminates per-call heap allocation. Public signature of `is_low_quality(&str) -> bool` unchanged; no call sites touched.
- **Breaking risk**: Zero. Isolated to `routing::quality`, no exported types, no call-site changes.
- **Security risk**: Zero. No auth, no IO, no user-facing surface.
- **Bottom line**: Safe to merge.

## Review History

### Review 1 — 2026-05-29T00:00:00Z
**Type**: Fresh
**Commit**: e9be8c375bf53ed254987349f2b2e5909515a7d9
**Summary**: Single-file Rust change to `routing/quality.rs` plus Cargo dependency declarations. Replaces naive `to_lowercase + starts_with` logic with a lazily-compiled Aho-Corasick DFA scanned over a 200-byte window. Adds 7 new regression test cases covering position-tolerance, empty-noise tokens, Unicode safety, and DFA construction.
**Gates**: CI pending (3 checks) | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (`src/openhuman/routing/quality.rs`)
**Red flags**: None
**Linked issues**: None stated
**PR-Issue alignment**: N/A — standalone perf/correctness improvement
**CodeRabbit dedup**: CR flagged 5 dead entries in `EMPTY_NOISE_TOKENS` ("yes.", "no.", "hmm.", "ok.", "ok!") that are shorter than MIN_CHARS=5 and never reached — skipped in review body.
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — single isolated module
**Dependency audit**: `aho-corasick = "1.1"` — BurntSushi-maintained, MIT/Unlicense, already transitive via `regex`. No concerns.
**Test coverage**: 14 tests (10 legacy + 4 new categories). Coverage gate passed in CI. Comprehensive edge-case coverage including UTF-8 boundary truncation and DFA construction smoke test.
**Impact scan**: Public signature unchanged. No callers modified.
**Findings**:
- [minor] `src/openhuman/routing/quality.rs` REFUSAL_PHRASES — `"i'm afraid i can"` is over-broad; matches "I'm afraid I can only give you three results" (constrained but non-refusing). Acceptable given documented false-positive bias, but `"i'm afraid i can't"` + `"i'm afraid i cannot"` would be more precise.
**Action taken**: COMMENT — code clean, CI pending. Will approve once CI is green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2870#pullrequestreview-4385056245
