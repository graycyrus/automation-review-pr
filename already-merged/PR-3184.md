# PR #3184 — fix(test): only source ~/.cargo/env when it exists

## Metadata
| Field | Value |
|-------|-------|
| Author | @erxiong0 |
| Branch | exp/architecture → main |
| Created | 2026-06-02 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3184 |
| Status | clean |
| Last reviewed commit | 215cee1c9993a89ddcc3b0f9951c51bc0bb2f958 |
| Last review date | 2026-06-02T08:54:00Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Fixes a critical test-runner bug where `source ~/.cargo/env` failed silently on machines without rustup (Homebrew-installed Rust). Under `set -e`, sourcing a missing file is fatal in non-interactive shells and `\|\| true` doesn't catch it — the shell exits before `\|\|` is evaluated. Tests never ran but looked like success. The fix guards with `[ -f "$HOME/.cargo/env" ]`. |
| Breaking risk | Zero | Test infrastructure only — no production code changes. |
| Security risk | Zero | Shell script guard for environment loading. |
| Bottom line | Safe to merge | Legitimate bug fix with correct implementation, CI 100% green. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Clear technical explanation, minimal focused change, not AI-generated. |
| Structural signals | None | Appropriate guard + correct comment disabling shellcheck for dynamic source. |
| Content signals | None | — |
| Behavioral signals | None | Single-purpose fix, well-scoped. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — test infrastructure only |
| User flow | Unchanged |
| Affected surfaces | N/A |
| Responsiveness | N/A |
| Accessibility | N/A |
| i18n | N/A |
| Performance | N/A |
| Edge states | N/A |
| Risk | None |
| Recommendation | Approved as-is |

## Review History

### Review 1 — 2026-06-02T08:54:00Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | 215cee1c9993a89ddcc3b0f9951c51bc0bb2f958 |
| Gates | CI pass \| Conflicts pass |
| Areas changed | Test infrastructure |
| Linked issues | None |
| PR-Issue alignment | N/A |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Single-file fix to `scripts/test-rust-with-mock.sh`. Wraps unconditional `source "$HOME/.cargo/env"` in `[ -f "$HOME/.cargo/env" ]` guard. Root cause: under `set -e`, sourcing a missing file is fatal in non-interactive shells and `\|\| true` doesn't catch it — exits before `\|\|` is evaluated. On machines without rustup, this caused tests to never run but still report success. |
| CodeRabbit dedup | N/A — CodeRabbit skipped review on this change. |
| Resolution actions | None — no prior findings to resolve. |
| Surrounding code checked | None — one-line logic fix, no dependencies. |
| Dependency audit | N/A |
| Test coverage | N/A — fix is to test infrastructure; CI includes Rust E2E which exercises this script directly. All green. |
| Impact scan | N/A |

#### Findings
None

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | APPROVE — code is correct, root cause is clear, all CI green including Rust E2E which directly exercises this script. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3184#pullrequestreview-4408130140 |
