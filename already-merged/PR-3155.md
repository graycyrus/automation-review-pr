# PR #3155 — fix(core): 16 MiB worker stack so subagent delegation doesn't crash the JSON-RPC server

## Metadata
| Field | Value |
|-------|-------|
| Author | @sanil-23 |
| Branch | fix/worker-stack-overflow → main |
| Created | 2026-06-01 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3155 |
| Status | clean |
| Last reviewed commit | dd212eb8c0c938f38f2e0409633a9245f178e1f0 |
| Last review date | 2026-06-01T14:57:00Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Sets `thread_stack_size(16 MiB)` on the tokio multi-thread runtime in `run_server_command`. Fixes a SIGABRT crash when the orchestrator delegates to a sub-agent — the nested async state machine overflows the default 2 MiB worker stack. One-liner fix with detailed explanatory comment. |
| Breaking risk | Zero | Runtime configuration only. No exported functions, types, DB schemas, or config formats changed. |
| Security risk | Zero | Stack size configuration introduces no attack surface. |
| Bottom line | Safe to merge | Correct, surgical fix for a real crash. Only blocker is CI not fully green. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | PR description is technically specific with concrete numbers (17.6 KB system prompt, spawn_depth=1, port numbers). Comment explains the exact failure mode. Reads like someone who actually debugged this. |
| Structural signals | None | |
| Content signals | None | |
| Behavioral signals | None | 9-line change, E2E coverage is appropriate for this type of runtime config fix. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — no frontend changes |
| User flow | Unchanged |
| Affected surfaces | N/A |
| Responsiveness | Not applicable |
| Accessibility | Not applicable |
| i18n | Not applicable |
| Performance | +14 MiB reserved stack per worker thread. Not committed memory. Acceptable for CLI/server context. |
| Edge states | Not applicable |
| Risk | None |
| Recommendation | Approve as-is once CI is green |

## Review History

### Review 1 — 2026-06-01T14:57:00Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | dd212eb8c0c938f38f2e0409633a9245f178e1f0 |
| Gates | CI fail (Rust Core Coverage failing, Frontend Coverage pending) \| Conflicts pass \| Feedback pass |
| Areas changed | Rust core — `src/core/cli.rs` (tokio runtime builder config) |
| Linked issues | None |
| PR-Issue alignment | N/A |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Single-file change adding `.thread_stack_size(16 * 1024 * 1024)` to the tokio multi-thread runtime builder in `run_server_command`. Fixes SIGABRT crash when subagent delegation causes a second full async agent turn to run on the same 2 MiB worker stack. Playwright lane 1/4 now passes. |
| CodeRabbit dedup | CodeRabbit approved with no inline comments — nothing to dedup. |
| Resolution actions | None |
| Surrounding code checked | `src/core/cli.rs` diff context |
| Dependency audit | No dependency changes |
| Test coverage | N/A — runtime config change exercised by existing Playwright `chat-harness-subagent` spec, which now passes. Unit tests cannot cover this. |
| Impact scan | No exported symbols changed. Tauri desktop runtime (`src-tauri/`) is a separate codepath and does NOT get this fix — acknowledged in PR as a follow-up. Should be filed as an issue. |

#### Findings
- [minor] `src/core/cli.rs` — The Tauri in-process desktop runtime is exposed to the same stack overflow but doesn't get this fix. PR notes this as a follow-up TODO. Should be a filed GitHub issue before merge to prevent it from being forgotten.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — code is clean and correct, but Rust Core Coverage is failing and Frontend Coverage is pending. Holding APPROVE until CI is fully green. Also flagged the Tauri follow-up gap. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3155#pullrequestreview-4402258819 |
