# PR #2889 — feat(composio): migrate GitHub provider to memory_tree pipeline (#2885)
- **Author**: @justinhsu1477
- **Branch**: feat/composio-github-memory-tree-ingest → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2889
- **Status**: clean
- **Last reviewed commit**: 7d27b5d486599b4d47c34f31f5d0d2ef3a155cf8
- **Last review date**: 2026-05-29T09:03:24Z

## Review History

### Review 1 — 2026-05-29T02:51:30Z
**Type**: Fresh
**Commit**: 1f35cf9358dc548c67aa5d38d463c3fde40608ec
**Summary**: Migrates the GitHub Composio provider from the legacy `MemoryClient::store_skill_sync` → `memory_docs` path to `ingest_pipeline::ingest_document` → `mem_tree_chunks`. Second in the four-PR #2885 series. Adds new `ingest.rs` module (474 lines) with 6 unit/async tests covering source_id stability, timestamp parsing, body rendering, URL extraction, and re-ingest correctness.
**Gates**: CI pending (Windows secrets ACL, E2E macOS/Windows/Linux, Tauri build, Rust Core Coverage, macOS smoke) | Conflicts pass | Unresolved CodeRabbit feedback (cursor advancement + error chain)
**Areas changed**: Rust core (memory_sync/composio/providers/github)
**Red flags**: None
**Linked issues**: #2885 (feat: migrate Notion/ClickUp/Linear/GitHub Composio providers off UnifiedMemory)
**PR-Issue alignment**: Fully aligned. Issue #2885 defines the 4-PR migration sequence; this PR delivers the GitHub provider step exactly as spec'd (new ingest.rs, routes through ingest_pipeline::ingest_document, stable source_id, regression test).
**CodeRabbit dedup**: Skipped cursor-advancement finding (provider.rs:333 — failed ingests silently advance cursor without retry) and error-chain formatting (ingest.rs:191 — {err} should be {err:#} for anyhow chain). Did not repeat either.
**Resolution actions**: None
**Surrounding code patterns checked**: Notion ingest pattern from #2887, provider.rs tracing field style
**Dependency audit**: No new dependencies
**Test coverage**: 6 tests in ingest.rs covering all new public/private functions; regression test pins the #2885 silent-failure mode explicitly. Solid.
**Impact scan**: `persist_single_item` left in place for Linear/ClickUp — no breakage to other providers. No exported function signatures changed.

## AI Summary
**What it does**: Rewires the GitHub Composio provider so synced issues/PRs land in `mem_tree_chunks` + `mem_tree_ingested_sources` instead of the legacy `memory_docs` table. Before this PR, every GitHub item synced by Composio was invisible to `memory.search`, `tree.read_chunk`, `tree.browse`, the agent recall path, and summary trees — the data existed but was unreachable by every modern retrieval surface. This PR fixes that by following the same ingest_pipeline pattern already proven by Slack, Gmail, and now Notion.

**Breaking risk**: Zero. Additive change — new ingest path added, old `persist_single_item`/`store_skill_sync` left intact for the remaining providers. No public API changes.

**Security risk**: Zero. Internal data pipeline only. No user-facing inputs, no auth surfaces, no new network calls beyond what the sync loop already makes.

**Bottom line**: Safe to merge — pending CI completion.

**Findings**:
- [minor] src/openhuman/memory_sync/composio/providers/github/ingest.rs:171 — `log::debug!` uses positional format strings; rest of codebase uses `tracing::debug!` with structured key=value fields. Swap to `tracing::debug!` for consistency.

**Action taken**: COMMENT (CI pending; tagged @justinhsu1477 to fix CI + address CodeRabbit cursor-advancement + error chain findings)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2889#pullrequestreview-4386246336

### Review 2 — 2026-05-29T03:51:30Z
**Type**: Continuation
**Commit**: 9764b31f1784b73a19ef455dd72243e5ac1e8054
**Summary**: Third commit (fix: hold cursor on ingest failure + preserve error chain) addresses CodeRabbit's two CHANGES_REQUESTED findings. (1) `had_ingest_failures` flag gates `advance_cursor` — cursor held when any item in the pass fails, preventing the delete-first pipeline from leaving edited issues with no chunks. (2) `{err:#}` replaces `{err}` in the anyhow wrap so the full context chain is preserved through `tracing::warn!(error = %e)`. Second performance commit (perf: skip chunk scan on fresh-issue ingest) gates `delete_chunks_by_source` behind `is_source_ingested`, turning the common fresh-ingest case from a full `source_kind` scan into a single indexed PK lookup. CodeRabbit re-approved after the fix.
**Gates**: CI still pending (Build Tauri App, Rust Core Coverage, E2E Appium macOS/Windows/Linux, Windows secrets ACL) | Conflicts pass | graycyrus [minor] thread still open (log::debug! → tracing::debug!)
**Areas changed**: ingest.rs (5 lines), provider.rs (25 lines)
**Red flags**: None
**Linked issues**: #2885
**PR-Issue alignment**: N/A (continuation)
**CodeRabbit dedup**: All CodeRabbit findings now resolved by code — cursor fix and error chain fix. CR re-approved.
**Resolution actions**: graycyrus [minor] thread (ingest.rs:171 log::debug!) not addressed — left open. CodeRabbit threads resolved by their own re-approval; did not touch bot threads.
**Surrounding code patterns checked**: provider.rs cursor advancement logic, ingest.rs error wrapping pattern
**Dependency audit**: N/A
**Test coverage**: 48/48 still passing per PR description; no new tests needed for the cursor gate (invariant documented via rustdoc on had_ingest_failures).
**Impact scan**: No signature changes. `had_ingest_failures` is local to sync loop.
**Findings**:
- [minor] src/openhuman/memory_sync/composio/providers/github/ingest.rs:171,180 — `log::debug!` with positional args still present; not addressed in third commit. Open from Review 1.
**Action taken**: COMMENT (CI pending; acknowledged CR fixes; flagged log::debug! holdover)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2889#pullrequestreview-4386467986

### Review 3 — 2026-05-29T09:03:24Z
**Type**: Continuation
**Commit**: 7d27b5d486599b4d47c34f31f5d0d2ef3a155cf8
**Summary**: Fourth commit (style: use tracing::debug! with structured fields) addresses the last open graycyrus [minor] finding — both debug sites in ingest.rs now use `tracing::debug!` with named key=value fields matching the codebase convention. No logic changes; purely a logging style fix.
**Gates**: CI still pending (Build Tauri App, Rust Core Tests + Coverage, E2E Appium macOS/Windows/Linux, Frontend Coverage, Windows secrets ACL) | Conflicts pass | All review threads now resolved
**Areas changed**: ingest.rs (logging style only)
**Red flags**: None
**Linked issues**: #2885
**PR-Issue alignment**: N/A (continuation)
**CodeRabbit dedup**: No new CodeRabbit findings (CR skipped this pass).
**Resolution actions**: graycyrus [minor] thread (PRRT_kwDORTXxrs6FkmuE) — confirmed fixed in 7d27b5d, replied + resolved. Thread was isOutdated=true confirming the code at that line changed.
**Surrounding code patterns checked**: None needed — single-line style change only.
**Dependency audit**: N/A
**Test coverage**: N/A — logging change only.
**Impact scan**: N/A — no signature changes.
**Findings**: None
**Action taken**: COMMENT (CI still pending; all threads resolved; code clean; moved to to-be-approved)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2889#pullrequestreview-4388179015
