# PR #2954 — feat(agent/multimodal): support [FILE:…] markers with text extraction (#2777)

## Metadata
| Field | Value |
|-------|-------|
| Author | @oxoxDev |
| Branch | `feat/2777-multimodal-file-attachments` → `main` |
| Created | 2026-05-29 |
| URL | https://github.com/tinyhumansai/openhuman/pull/2954 |
| Status | clean |
| Last reviewed commit | de546c05131bcd20f59146846e95333efb2e74fc |
| Last review date | 2026-06-01T16:17:20Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Extends `agent/multimodal.rs` to resolve `[FILE:…]` markers (PDF/TXT/CSV/MD extracted inline; ZIP/XLSX/DOCX/PPTX as metadata-only references). Also fixes two pre-existing defects: the production web-chat path (`Agent::turn`) never ran the multimodal pipeline at all, and per-turn marker caps were summing across history rather than checking only the latest user message. |
| Breaking risk | Low | `MultimodalFileConfig` added with `serde(default)` — existing TOMLs parse unchanged. `prepare_messages_for_provider` gains a third parameter; all call sites updated. `rag-pdf` feature flag removed (was optional, now unconditional). |
| Security risk | Low | `for_untrusted_channel_input()` guard correctly disables file markers on channel-sourced (Slack/Discord/Telegram) turns. Desktop/web-chat path allows local paths by design (user owns filesystem). Remote fetch opt-in. `data:` URIs rejected. |
| Bottom line | Safe | Safe to merge once CI is green. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | — |
| Structural signals | None | Clean abstractions, appropriate complexity for the feature scope. `FilePayload` enum is the right model. |
| Content signals | None | Named types match domain semantics throughout. |
| Behavioral signals | None | 31 new unit tests, existing 8 image tests untouched, live end-to-end verified with canary tokens. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — no frontend changes in this PR |
| User flow | Unchanged |
| Affected surfaces | N/A |
| Responsiveness | Not applicable |
| Accessibility | Not applicable |
| i18n | Not applicable |
| Performance | Not applicable |
| Edge states | Not applicable |
| Risk | None |
| Recommendation | N/A — backend-only change |

## Review History

### Review 1 — 2026-06-01T16:17:20Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | de546c05131bcd20f59146846e95333efb2e74fc |
| Gates | CI fail (Rust Core Coverage + E2E lane 1/4) \| Conflicts pass \| Feedback pass |
| Areas changed | Rust core (multimodal, config, bus, tool_loop, session/turn, channels/runtime), tests |
| Linked issues | #2777, #1535 |
| PR-Issue alignment | All #2777 acceptance criteria met: FILE markers parsed, text extracted from PDF/TXT/CSV/MD, binary-only reference for ZIP/XLSX/DOCX/PPTX, size+MIME rejection with actionable errors, image marker parity unchanged, unit test coverage >= 80% |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds file-marker pipeline as parallel to existing image pipeline. Wires `prepare_messages_for_provider` into `Agent::turn` for the first time (fixing the production web-chat path gap). Fixes per-turn marker counting to inspect only the latest user message. Adds `MultimodalFileConfig` with `for_untrusted_channel_input()` hardening for channel dispatches. |
| CodeRabbit dedup | CodeRabbit flagged PreparedMessages token budget interaction post-expansion — noted in body but not repeated as inline. |
| Resolution actions | None — fresh review |
| Surrounding code checked | `session/turn.rs` integration, `channels/runtime/dispatch.rs` untrusted guard, `config/schema/tools.rs` MultimodalFileConfig |
| Dependency audit | `pdf-extract 0.10` promoted from optional to unconditional. Transitive deps: lopdf, adobe-cmap-parser, cff-parser, postscript, euclid 0.20 (separate from existing 0.22). All MIT/Apache-2.0. ~2MB binary size addition per PR description. Actively maintained. |
| Test coverage | 31/31 unit tests pass locally. 16 new file-marker tests, 8 existing image tests green. Public integration tests 5/5. Per-turn counting regression test present. |
| Impact scan | `prepare_messages_for_provider` signature changed (third param added) — all call sites in `tool_loop.rs`, `bus.rs`, `channels/runtime/dispatch.rs`, `triage/evaluator.rs`, `subagent_runner/ops.rs`, and test fixtures updated. No missed callers. |

#### Findings
- [minor] `multimodal.rs` `file_mime_known()` — first clause `file_mime_from_extension(mime).is_some()` receives a MIME type string and can never match extension patterns; always false, dead code.
- [minor] `multimodal.rs` — Entry-point and per-file-resolution logs use `debug!` but PR description says INFO; operators greping `[multimodal]` won't see them without DEBUG enabled.
- [minor] `multimodal.rs` `detect_file_mime()` — remote files have no `path_hint`; `.xlsx` from a server sending `application/octet-stream` degrades to `application/zip` mime in the reference block. Behaviour correct (still binary reference), reported MIME is misleading.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — CI failing (Rust Core Coverage pre-existing test failure on "reasoning-v1" model assertion; E2E lane 1/4 ECONNREFUSED infrastructure flakiness). Code itself is clean; only minor findings. Will approve once CI is green. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/2954#pullrequestreview-4402837836 |
