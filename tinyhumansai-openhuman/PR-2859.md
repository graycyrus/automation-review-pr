# PR #2859 — fix(channels): render Lark/DingTalk/iMessage logos + add connect forms
- **Author**: @EvanCarson
- **Branch**: fix/channel-logos-lark-dingtalk-2854 → main
- **Created**: 2026-05-28
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2859
- **Status**: clean
- **Last reviewed commit**: c8090b8c85136ef580e4fd2ed4f4a8596623f190
- **Last review date**: 2026-05-29T00:00:00Z

## AI Summary
- **What it does**: Fixes missing logos on Lark/Feishu, DingTalk, and iMessage channel cards in Settings → Connections → Channels. Unifies two divergent `CHANNEL_ICONS` maps into a single `renderChannelIcon` helper in `channelIcon.tsx`, adds brand icon components (LarkIcon, DingTalkIcon, IMessageIcon), fixes config panel showing raw i18n keys by falling back to core-provided `display_name`/`description`, and adds a schema-driven `CredentialChannelConfig` component so Lark and DingTalk can actually be connected from the UI.
- **Breaking risk**: Zero — purely additive frontend UI changes. No exported types, shared state shapes, RPC methods, or API signatures changed.
- **Security risk**: Zero — credentials flow through existing `channelConnectionsApi` RPC. No new external network dependencies, no secrets in source.
- **Bottom line**: Safe to merge.

## Review History

### Review 1 — 2026-05-29T00:00:00Z
**Type**: Fresh
**Commit**: c8090b8c85136ef580e4fd2ed4f4a8596623f190
**Summary**: Frontend-only PR (0 Rust lines changed) across 15 files (+631/-38). Unifies channel icon rendering, adds three icon components, fixes i18n fallback in `ChannelConfigPanel`, and adds a new `CredentialChannelConfig` component wired for lark/dingtalk. Comprehensive Vitest tests added across all new/changed components.
**Gates**: CI FAILING (Rust Core Tests + Quality unrelated to this PR) | Conflicts pass | No prior graycyrus changes requested
**Areas changed**: Frontend (app/src/components/channels/), public assets
**Red flags**: `--no-verify` pre-push hook bypass (justified but process concern)
**Linked issues**: Closes #2854
**PR-Issue alignment**: All 5 acceptance criteria met — logos render, theme safe, packaged build safe (public dir pattern), regression coverage added, diff coverage ≥ 80%.
**CodeRabbit dedup**: Two findings (aria-hidden on emoji fallback, stuck `connecting` state on connect exception) — both already addressed in commit 4272a7b. Skipped.
**Resolution actions**: None (first review)
**Surrounding code patterns checked**: channelIcon.tsx, CredentialChannelConfig.tsx, ChannelFieldInput.tsx, ChannelConfigPanel.tsx
**Dependency audit**: No new dependencies
**Test coverage**: 4 new test files, 218 tests passing per PR validation. Happy path + failure paths covered.
**Impact scan**: YuanbaoIcon import removed from ChannelSelector/ChannelSetupModal — now managed inside channelIcon.tsx. Both consumers verified. No other callers affected.
**Findings**:
- [minor] CredentialChannelConfig.tsx:110 — Required boolean field validation: `if (field.required && !val)` treats unset checkbox (`''`) as invalid even though unchecked is a valid boolean state. No current required boolean fields in Lark/DingTalk so not a live bug, but should be hardened.
- [minor] CredentialChannelConfig.tsx:124 — Restart-failure path falls through to `upsertChannelConnection` with `status: 'connected'` even when `restartCoreProcess()` throws. Credentials are saved but channel isn't active — badge is misleading.
- [minor] Process: `--no-verify` pre-push bypass. Justified (ripgrep not in env, unrelated dir scan), CI covers same checks, but should be a last resort.
**Action taken**: COMMENT — CI failing (Rust Core Tests unrelated to frontend changes) + pending E2E. Flagged 2 minor inline issues. Will approve once CI green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2859#pullrequestreview-4383913756
