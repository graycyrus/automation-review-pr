# PR #3072 — fix(scripts): reject invalid mock API ports

- **Author**: @alexzhu0
- **Branch**: `codex/OH-2611-mock-api-port-arg` → `main`
- **Created**: 2026-05-31T05:43:20Z
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3072
- **Status**: clean
- **Last reviewed commit**: e85596ace1d656cc30b1151592400437c67c9411
- **Last review date**: 2026-06-01T13:05:54Z

## Review History

### Review 1 — 2026-06-01T13:05:54Z

**Type**: Fresh

**Commit**: e85596ace1d656cc30b1151592400437c67c9411

**Summary**: Fixed argument validation in mock API server script. Now properly rejects invalid `--port` arguments (non-integer, out-of-range, missing) before server startup instead of silently falling back to default port. Adds `--help` flag and comprehensive Node tests covering all validation paths.

**Gates**: 
- CI pass: FAILING (Rust Core Coverage) — blocking APPROVE, using COMMENT instead
- Conflicts pass: ✅
- Unresolved feedback pass: ✅

**Areas changed**: Scripts (Node.js helper), Tests

**Red flags**: None

**Linked issues**: Related to #2611

**PR-Issue alignment**: Fixes malformed mock API port argument handling as described in PR body.

**CodeRabbit dedup**: CodeRabbit flagged missing `<= 65535` upper-bound check on port validation. Already fixed in this commit — validation properly checks `port <= 65535` and test includes `['--port', '65536']` case.

**Resolution actions**: None — CodeRabbit feedback already addressed.

**Surrounding code patterns checked**: None — isolated script change.

**Dependency audit**: N/A — no dependencies changed.

**Test coverage**: Excellent — Node tests cover help flag, invalid ports (non-integer, out-of-range), missing ports, flag-shaped arguments, and verify no listener opened on error paths.

**Impact scan**: Internal helper script only. No product runtime impact. Developer/test impact: malformed mock backend port arguments now fail fast with clear error messages instead of silently using default port.

## AI Summary

**What it does**: Fixes a bug where `node scripts/mock-api-server.mjs --port nope` would silently fall back to the default port instead of failing fast. Now validates explicit port arguments (1-65535) before server startup, with clear error messages and `--help` support.

**Breaking risk**: Zero — internal helper script, no API/type/config changes.

**Security risk**: Zero — proper input validation on process.argv flag parsing, no injection surfaces.

**Bottom line**: Safe to merge once CI passes.

**Findings**: None — code is clean, focused, correct.

**Action taken**: COMMENT — code is approved pending CI resolution. Posted review noting CI blocker and readiness to approve once Rust Core Coverage passes.

**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3072#pullrequestreview-4401410711
