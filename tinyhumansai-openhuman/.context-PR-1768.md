# Intelligence Context — PR #1768

## PR Metadata
- **Title**: Add agent task orchestration
- **Author**: @Zavianx
- **Branch**: feat/agent-task-orchestration → main
- **URL**: https://github.com/tinyhumansai/openhuman/pull/1768
- **Review type**: Fresh
- **Latest commit**: 644a350ee47c93b05e5d7ccfbf008491eb1459a1
- **Commit count**: 8 (1 feat + 2 fix + 4 ci + 1 fix)
- **Diff size**: 63 files, +4606 -286 lines (6629 diff lines)

## PR Description
- Add persisted agent task boards, wire `todowrite` updates into board events/RPCs, and show a kanban board in conversations.
- Add a bounded `spawn_parallel_agents` tool for concurrent subagent task execution with per-task progress/error summaries.
- Add persisted agent profiles with backend RPCs, prompt/session routing, tool visibility overrides, and a conversation profile selector/create panel.
- Update E2E mocks/scripts for current onboarding, billing handoff, and Linux CEF execution.

### Testing claimed
- `cargo fmt --all`
- `pnpm format:check`
- `pnpm typecheck`
- `pnpm lint` (passes with 32 existing warnings)
- `pnpm test:coverage`
- `pnpm test:rust`
- `xvfb-run -a pnpm --filter openhuman-app test:e2e` (login: 12 passing; auth/access: 9 passing)
- Pre-push hook: `format:check`, `lint`, `compile`, `rust:check`, `lint:commands-tokens`

## Red Flags
- **Very large diff**: 4606 additions across 63 files — three features + E2E updates bundled in one PR. This is a substantial PR that would benefit from splitting into separate PRs per feature (task boards, parallel agents, agent profiles, E2E updates).
- **Multiple unrelated changes bundled**: Task boards, parallel agents, agent profiles, and E2E mock/onboarding updates are conceptually independent features.
- **No labels** on the PR.

## Linked Issues + Acceptance Criteria

### Issue #1756 — Add support for parallel agents
**Acceptance criteria:**
- [ ] Multiple workers can run concurrently
- [ ] Parent can track children (status and result)
- [ ] Safe boundaries exist (avoid overlap/conflicts)
- [ ] Failures are isolated (one worker failing doesn't corrupt others)
- [ ] Results can be merged (parent collects/reconciles child outputs)
- [ ] Regression coverage added
- [ ] Diff coverage >= 80%

### Issue #1757 — Add a kanban board for agent task tracking
**Acceptance criteria:**
- [ ] Board exists — users can view a kanban board
- [ ] Agent can update tasks — create, move, complete cards
- [ ] State persists — survives refresh/restart
- [ ] Blockers are visible — agent can mark blocked items
- [ ] Progress is readable — quick view of current work
- [ ] Regression coverage added
- [ ] Diff coverage >= 80%

### Issue #1758 — Add multiple agent profiles
**Acceptance criteria:**
- [ ] Profiles are selectable — user can choose among multiple profiles
- [ ] Profiles affect behavior — prompt, tools, permissions change
- [ ] Defaults are sane — new tasks pick a sensible default
- [ ] Profiles persist — survive restart and app updates
- [ ] Profile behavior is testable — unit/E2E tests cover selection and execution differences
- [ ] Diff coverage >= 80%

## PR-Issue Alignment (three-way verification)

### #1756 (Parallel agents)
- **PR claims**: bounded `spawn_parallel_agents` tool for concurrent subagent execution
- **Code delivers**: `spawn_parallel_agents.rs` (735 lines) with validation, fan-out via `join_all`, per-task results, ownership boundaries, progress events
- **Alignment**: Good — meets most criteria. Workers run concurrently via `join_all`. Parent tracks via structured results. Ownership boundaries exist but are advisory (not enforced). Failures isolated per-task. Tests cover validation but not actual concurrent execution (no real subagent runs in tests).
- **Gap**: No E2E test for parallel execution behavior. Coverage is unit-level only.

### #1757 (Task board)
- **PR claims**: persisted task boards, todowrite integration, kanban UI
- **Code delivers**: `task_board.rs` (470 lines), `TaskKanbanBoard.tsx` (107 lines), `todowrite.rs` wired to persist boards, socket events for live updates, thread API endpoints
- **Alignment**: Good — meets all criteria. Board persists on disk, agent updates via todowrite, UI renders kanban with drag/move and rollback, blockers visible.
- **Gap**: No drag-and-drop — only arrow button moves. Issue says "reorder" but reordering within a column is not implemented.

### #1758 (Agent profiles)
- **PR claims**: persisted profiles, backend RPCs, prompt/session routing, UI selector/create
- **Code delivers**: `profiles.rs` (708 lines), RPC handlers (schemas.rs), UI select/create form, session agent routing by profile
- **Alignment**: Good — profiles selectable, affect routing/prompts/tools, persist to JSON, built-in defaults exist. Tests cover CRUD operations.
- **Gap**: No E2E test for profile behavior differences. No profile editing (only create). No profile deletion from UI.

### Scope drift
The E2E changes (onboarding flow updates, billing handoff, CEF sandbox logic, Appium install, mock API stubs) are significant but unrelated to the three issues. They appear to be maintenance work bundled into this PR. Not blocking, but adds review burden.

## Prior Review Context (continuation only)
N/A — Fresh review.

## Changed Files

### Rust core — new features
- `src/openhuman/agent/profiles.rs` — NEW: agent profile store, CRUD, normalisation, prompt section
- `src/openhuman/agent/task_board.rs` — NEW: per-thread task board store, normalisation
- `src/openhuman/agent/schemas.rs` — profile RPC handlers (list, select, upsert, delete)
- `src/openhuman/agent/progress.rs` — new `TaskBoardUpdated` variant
- `src/openhuman/agent/mod.rs` — module declarations
- `src/openhuman/agent/harness/session/builder.rs` — `from_config_for_agent_with_profile`
- `src/openhuman/tools/impl/agent/spawn_parallel_agents.rs` — NEW: parallel agent tool
- `src/openhuman/tools/impl/agent/todo_write.rs` — wired to persist task boards + progress events
- `src/openhuman/tools/impl/agent/mod.rs` — module/export
- `src/openhuman/tools/ops.rs` — register `SpawnParallelAgentsTool`
- `src/openhuman/tools/ops_tests.rs` — registration test

### Rust core — routing/events
- `src/openhuman/channels/providers/web.rs` — profile-aware session building, task board events
- `src/openhuman/channels/providers/web_tests.rs` — updated signatures
- `src/openhuman/channels/bus.rs` — updated `start_chat` call
- `src/openhuman/channels/proactive.rs` — `task_board: None` field
- `src/openhuman/channels/providers/presentation.rs` — `task_board: None` field
- `src/core/socketio.rs` — `task_board` field, `profile_id` in payload

### Rust core — thread/turn state
- `src/openhuman/threads/schemas.rs` — task board RPC handlers (get, put)
- `src/openhuman/threads/schemas_tests.rs` — handler tests
- `src/openhuman/threads/turn_state/types.rs` — `task_board` field in TurnState
- `src/openhuman/threads/turn_state/mirror.rs` — observe TaskBoardUpdated
- `src/openhuman/threads/turn_state/mirror_tests.rs` — test coverage

### Rust core — other
- `src/api/config.rs` — test refactoring (env_lock helper, EnvSnapshot usage)
- `src/openhuman/agent/triage/escalation.rs` — test reliability (wait_for_events helper)
- `src/openhuman/screen_intelligence/tests.rs` — dual lock guard
- `src/openhuman/service/restart.rs` — test event filtering
- `src/openhuman/agent/agents/orchestrator/agent.toml` — add `spawn_parallel_agents` to tool list

### Frontend — new features
- `app/src/types/agentProfile.ts` — NEW: AgentProfile, AgentProfilesResponse types
- `app/src/types/turnState.ts` — TaskBoard, TaskBoardCard types + response types
- `app/src/services/api/agentProfilesApi.ts` — NEW: profile RPC client
- `app/src/services/api/agentProfilesApi.test.ts` — NEW: tests
- `app/src/services/api/threadApi.ts` — getTaskBoard, putTaskBoard
- `app/src/services/api/threadApi.test.ts` — tests
- `app/src/services/chatService.ts` — task_board_updated event, profileId in chatSend
- `app/src/services/__tests__/chatService.test.ts` — tests
- `app/src/store/chatRuntimeSlice.ts` — taskBoardByThread state
- `app/src/store/__tests__/chatRuntimeSlice.test.ts` — tests
- `app/src/store/coreModeSlice.ts` — E2E default core mode
- `app/src/store/coreModeSlice.test.ts` — tests
- `app/src/pages/Conversations.tsx` — profile selector, task board, create form
- `app/src/pages/__tests__/Conversations.render.test.tsx` — tests
- `app/src/pages/conversations/components/TaskKanbanBoard.tsx` — NEW: kanban UI
- `app/src/pages/conversations/components/__tests__/TaskKanbanBoard.test.tsx` — NEW: tests
- `app/src/providers/ChatRuntimeProvider.tsx` — onTaskBoardUpdated handler
- `app/src/providers/__tests__/ChatRuntimeProvider.test.tsx` — tests

### Frontend — config/setup
- `app/src/utils/config.ts` — E2E config exports
- `app/src/utils/tauriCommands/core.ts` — E2E restart-as-reload
- `app/src/utils/tauriCommands/core.test.ts` — tests
- `app/src/vite-env.d.ts` — env type declarations
- `app/src/test/setup.ts` — mock config additions
- `app/src/components/settings/panels/__tests__/DeveloperOptionsPanel.test.tsx` — mock fix

### E2E
- `app/test/e2e/helpers/shared-flows.ts` — onboarding/billing flow updates
- `app/test/e2e/specs/auth-access-control.spec.ts` — billing handoff tests
- `app/test/e2e/specs/login-flow.spec.ts` — updated onboarding markers
- `app/scripts/e2e-build.sh` — E2E build env vars
- `app/scripts/e2e-run-session.sh` — CEF sandbox/workspace logic

### CI
- `.github/workflows/coverage.yml` — timeout 30→45 min
- `.github/workflows/e2e-agent-review.yml` — Appium install refactor
- `.github/workflows/e2e.yml` — Appium install refactor (3 jobs)
- `app/.prettierignore` — ignore e2e artifacts

### Mock API
- `scripts/mock-api/routes/integrations.mjs` — Composio toolkits + execute stubs

## Classification + Checklist

### Areas detected: Rust core, Frontend, Event bus, CI/workflows, Tests, E2E

#### Rust core checklist
- [ ] New functionality in dedicated subdirectory (`openhuman/<domain>/mod.rs`) — profiles.rs and task_board.rs are in `openhuman/agent/` alongside existing files
- [ ] No standalone `*.rs` at `src/openhuman/` root — PASS: files are in `agent/` subdirectory
- [ ] Controller-only exposure (no domain branches in `cli.rs`/`jsonrpc.rs`) — PASS: exposed via schemas.rs controller pattern
- [ ] Light `mod.rs` (exports only, logic in `ops.rs`/`store.rs`/`types.rs`) — profiles.rs and task_board.rs contain both store and types in one file; acceptable for module size
- [ ] Debug logging with grep-friendly prefixes — PASS: `[agent:profiles]`, `[agent:task_board]`, `[rpc][agent]`, `[rpc][task_board]` prefixes used
- [ ] No secrets/PII in logs — VERIFY: workspace paths are logged (`[todowrite][task_board] persisted` with workspace name); CodeRabbit flagged this
- [ ] `tempfile::NamedTempFile` for atomic writes — PASS: both stores use tempfile→persist pattern

#### Frontend checklist
- [ ] No dynamic imports (`import()`, `React.lazy`, `await import()`) — PASS
- [ ] Config read from `config.ts`, not `import.meta.env` directly — **FLAG**: `coreModeSlice.ts` imports `E2E_DEFAULT_CORE_MODE` from `config.ts`, which is correct, but CodeRabbit noted this pattern
- [ ] State in Redux, not ad-hoc localStorage — **FLAG**: Agent profiles state is in component `useState`, not Redux. Task board state is in Redux (`taskBoardByThread`). Profile state should arguably be in Redux for consistency.
- [ ] Tauri access via `isTauri()` or try/catch `invoke()`, not `window.__TAURI__` — PASS

#### Event bus checklist
- [ ] Typed pub/sub, singleton API, convention naming — PASS: `task_board_updated` event follows snake_case convention

#### CI checklist
- [ ] Coverage gate >= 80% on changed lines — PR claims all checks pass

## Surrounding Code Patterns

### src/openhuman/agent/
**Files read**: dispatcher.rs, hooks.rs
- **Naming**: PascalCase structs with suffixes, snake_case free functions, SCREAMING_SNAKE for constants
- **Error handling**: `anyhow::Result` at async boundaries, `Result<T, String>` synchronous. Tool errors returned as `Ok(ToolResult::error(...))` to keep agent loop non-fatal
- **Imports**: `crate::openhuman::*` hierarchy, `async_trait`, `Arc` for shared ownership
- **Logging**: Mixed `tracing::debug!` (structured) and `log::debug!` (format-string). Structured fields use naked `key = value`. Lowercase messages.
- **Tests**: Split into separate `_tests.rs` files via `#[cfg(test)] #[path = "..."] mod tests;`. `#[tokio::test]` for async, `tempfile::TempDir` for fs tests.

### src/openhuman/tools/impl/agent/
**Files read**: spawn_subagent.rs, delegate.rs
- **Naming**: `<Verb><Noun>Tool` structs. Manual `Default` impl.
- **Error handling**: All `execute` return `anyhow::Result<ToolResult>` but almost never `Err(...)` — validation failures return `Ok(ToolResult::error(...))`. Error details stripped before surfacing to model.
- **Imports**: `crate::openhuman::tools::traits::{Tool, ToolResult, PermissionLevel}`, event bus publishing via `crate::core::event_bus`
- **Logging**: `tracing::debug!` with `target: "spawn_subagent"`, also `log::debug!` with `[spawn_subagent]` prefix (mixed)
- **Tests**: Inline `mod tests` with `use super::*`. `tempfile::TempDir`. Intent-named: `missing_agent_id_returns_error`

### src/openhuman/threads/
**Files read**: ops.rs, turn_state/store.rs
- **Naming**: RPC handlers are flat `snake_case` verbs. Store structs `<Noun>Store`. Module-level `LOG_PREFIX` constants.
- **Error handling**: RPC returns `Result<RpcOutcome<...>, String>`. Store returns `Result<T, String>`. Failure messages include path context.
- **Imports**: `chrono::Utc::now().to_rfc3339()` for timestamps, `uuid::Uuid::new_v4()`, `once_cell::sync::Lazy` + `parking_lot::Mutex`, `tempfile::NamedTempFile` for atomic writes
- **Logging**: `tracing::debug!` with named fields in ops.rs, `log::debug!` with LOG_PREFIX in store.rs

### src/openhuman/channels/
**Files read**: bus.rs, providers/slack.rs
- **Naming**: `<Domain><EventType>Subscriber`, `<Platform>Channel`. Private `StreamingState`/`TypingState`.
- **Error handling**: Network errors `anyhow::Result` at trait boundary, caught and logged internally. Latch booleans prevent repeated failures.
- **Imports**: `async_trait`, `serde_json::json!` for JSON bodies
- **Logging**: Exclusively `tracing::info!/warn!/error!/debug!` (no `log::` macros). `[channel-inbound]` tag.

### app/src/services/api/
**Files read**: authApi.ts, tunnelsApi.ts
- **Naming**: `<domain>Api.ts` files, camelCase functions. Namespace object exports (`tunnelsApi`).
- **Error handling**: `callCoreRpc` errors propagate; callers responsible. No swallowing at API layer.
- **Tests**: `vi.mock` at module scope, `mockCallCoreCommand.mockReset()` in `beforeEach`, three cases per function.

### app/src/store/
**Files read**: accountsSlice.ts, threadSlice.ts
- **Naming**: `<domain>Slice.ts`. Thunks as verb phrases. Selectors as `select<What>`.
- **Error handling**: `rejectWithValue(error.message)` in thunks. Non-critical calls wrapped in inner try/catch.
- **State**: `initialState` with explicit inline interface. Immer mutations. `resetUserScopedState` wired in every slice.

### app/src/pages/conversations/components/
**Files read**: AgentMessageBubble.tsx, ToolTimelineBlock.tsx
- **Naming**: PascalCase matching. Named exports only. Props inline.
- **Tests**: `@testing-library/react` + vitest. Real Redux Provider. `data-testid` selectors.

### app/src/types/
**Files read**: thread.ts, intelligence.ts
- **Naming**: camelCase files, `interface` for objects, `type` for unions. Named exports only.

### app/src/providers/
**Files read**: CoreStateProvider.tsx, SocketProvider.tsx
- **Naming**: PascalCase files. `<Domain>Context`, `use<Domain>` hooks.
- **Error handling**: `.catch(err => log(...))` for non-fatal. `sanitizeError` for logging.
- **State**: Local `useState` + `useRef`, not Redux. `useCallback` with explicit deps. `debugFactory` for structured logging.

## CodeRabbit Dedup

CodeRabbit posted 3 reviews with 22 total actionable comments. Key findings:

**Major (7):**
- `src/openhuman/agent/profiles.rs:181` — Add verbose diagnostics logging with stable prefixes for all store operations → **Already addressed** in latest commits (tracing::debug throughout)
- `src/openhuman/agent/profiles.rs:125` — Add verbose diagnostics for task board operations → **Already addressed**
- `src/openhuman/agent/schemas.rs:493` — Add RPC diagnostics for profile handlers → **Already addressed** (request_id + tracing in all handlers)
- `src/openhuman/agent/task_board.rs:125` — Add persistence-path diagnostics → **Already addressed**
- `src/openhuman/agent/task_board.rs:140` — Reject blank `thread_id` before building board file paths → **Already addressed** (`validate_thread_id` function added)
- `src/openhuman/channels/providers/web.rs:1204` — Add diagnostic logging for task board update event → **Already addressed** (log::debug in bridge)
- `src/openhuman/threads/schemas.rs:397` — Align task board schema outputs with actual handler payload key → **Needs verification**: schema output name is "taskBoard" and handler returns `json!({ "taskBoard": ... })` — appears aligned
- `src/openhuman/threads/schemas.rs:560` — Add RPC diagnostics to task board handlers → **Already addressed**
- `src/openhuman/tools/impl/agent/todo_write.rs:170` — Don't fully hide task-board persistence failures from tool output → **Already addressed** (returns `ToolResult::error` on persist failure)
- `src/openhuman/tools/impl/agent/todo_write.rs:249` — Redact the workspace path from debug event → **NOT addressed**: `persist_thread_board` logs `workspace = %workspace_name` which is the last path component, not the full path. Partial mitigation but CodeRabbit flagged it.
- `src/openhuman/tools/impl/agent/todo_write.rs:254` — (continuation of above)

**Minor (11):**
- `app/src/services/api/agentProfilesApi.ts:13` — Handle undefined `data` in unwrapEnvelope → **Already addressed** (throws on undefined)
- `app/src/services/api/threadApi.ts:158` — Add null safety for taskBoard field → **Already addressed** (optional chaining `data?.taskBoard ?? null`)
- `app/src/store/coreModeSlice.ts:56` — Read E2E config via `config.ts` not `import.meta.env` → **Already addressed** (imports from config.ts)
- `app/test/e2e/helpers/shared-flows.ts:255` — Use polling for post-fallback billing verification → **Already addressed** (polling loop added)
- `app/test/e2e/specs/auth-access-control.spec.ts:217` — Add failure diagnostics to billing assertions → **Already addressed** (`expectBillingMarkers` with tree dump)
- `src/openhuman/agent/harness/session/builder.rs:853` — Add diagnostic logging for profile prompt injection → **Already addressed** (log::debug with suffix_chars)
- `src/openhuman/agent/task_board.rs:180` — Recompute order after filtering empty-title cards → **Already addressed** (order recomputed in normalise_board)
- `app/src/store/coreModeSlice.test.ts:75` — Guarantee unmock cleanup even when test fails → **Already addressed** (try/finally with doUnmock)
- `src/openhuman/agent/schemas.rs:837` — Restore previous OPENHUMAN_WORKSPACE in test cleanup → **Already addressed** (WorkspaceEnvGuard with Drop impl)
- `src/openhuman/threads/schemas_tests.rs:251` — Preserve prior OPENHUMAN_WORKSPACE → **Already addressed** (WorkspaceEnvGuard)
- `src/openhuman/tools/impl/agent/spawn_parallel_agents.rs:309` — Add comprehensive diagnostic logging to execute method → **Already addressed** (tracing::debug throughout)

**Outside diff range (2):**
- `app/scripts/e2e-run-session.sh:316-323` — Unconditional `--no-sandbox` on Linux → **Already addressed** (conditional logic based on uid/helper)
- `app/src/pages/Conversations.tsx:123-179` — (from second review round, details truncated)

**Summary**: Most CodeRabbit feedback has been addressed in the follow-up commits. Remaining: workspace path in todowrite debug log (minor).

## Dependency Audit
N/A — no dependency changes. No changes to Cargo.toml, package.json, Cargo.lock, or pnpm-lock.yaml.

## Test Coverage

### New logic with tests:
| Module | Test file | Coverage assessment |
|--------|-----------|-------------------|
| `profiles.rs` (708 lines) | Inline `mod tests` (12 tests) | Good: CRUD, normalisation, fallbacks, prompt section rendering |
| `task_board.rs` (470 lines) | Inline `mod tests` (8 tests) | Good: roundtrip, normalise, blank ID, corrupt file |
| `spawn_parallel_agents.rs` (735 lines) | Inline `mod tests` (6 tests) | Moderate: metadata, validation, boundary text. **No test of actual concurrent execution** — all tests validate parameter rejection or immediate failures |
| `todo_write.rs` (additions) | Inline (4 new tests) | Good: blockers, normalisation, persistence, progress emission, failure reporting |
| `TaskKanbanBoard.tsx` (107 lines) | `__tests__/TaskKanbanBoard.test.tsx` (52 lines) | Moderate: renders columns/cards, move callback. No disabled state test. |
| `agentProfilesApi.ts` (50 lines) | `agentProfilesApi.test.ts` (78 lines) | Good: list, select, upsert, delete, envelope rejection |
| `threadApi.ts` (additions) | `threadApi.test.ts` (additions) | Good: get/put task board, null envelope |
| `chatService.ts` (additions) | `chatService.test.ts` (additions) | Good: event subscription, profileId in send |
| `chatRuntimeSlice.ts` (additions) | `chatRuntimeSlice.test.ts` (additions) | Good: store/hydrate/clear task board |
| `Conversations.tsx` (additions) | `Conversations.render.test.tsx` (additions) | Good: profile create, duplicate validation, board move rollback |

### Gaps:
- **spawn_parallel_agents**: No test of actual concurrent subagent execution — only input validation. The acceptance criteria for #1756 says "Multiple workers can run concurrently" which isn't covered by tests.
- **Profile behavior differences**: No test verifying that selecting a profile actually changes agent behavior (model, tools, prompt). Unit tests cover CRUD but not the integration point in `build_session_agent`.
- **TaskKanbanBoard**: No test for disabled prop behavior.

## Impact Scan

### Changed function signatures

**`start_chat`** (web.rs) — added `profile_id: Option<String>` parameter
- Callers updated: `bus.rs`, `socketio.rs`, `web_tests.rs`, `channel_web_chat` — all pass `None` or forward the new param. **PASS**

**`run_chat_task`** — added `profile_id: Option<String>`
- Internal function, only called from `start_chat`. **PASS**

**`build_session_agent`** — added `target_agent_id: &str` and `profile: &AgentProfile`
- Called from `run_chat_task` only. **PASS**

**`channel_web_chat`** — added `profile_id: Option<String>`
- Called from `handle_chat` in web.rs and directly from `socketio.rs`. Both updated. **PASS**

**`pick_target_agent_id`** — changed from `fn(&Config) -> &'static str` to `fn(&Config, &AgentProfile) -> String`
- Internal function, only called from `run_chat_task`. **PASS**

**`Agent::build_session_agent_inner`** — added `profile_prompt_suffix: Option<String>`
- Internal method, called from `from_config_for_agent`, `from_config_for_agent_with_reflection_chunks`, and new `from_config_for_agent_with_profile`. All updated. **PASS**

### Changed/removed exports
- `spawn_parallel_agents` module + `SpawnParallelAgentsTool` — new export
- `profiles` module + types — new exports
- `task_board` module + types — new exports
- `setTaskBoardForThread`, `clearTaskBoardForThread` — new Redux actions
- `ChatTaskBoardUpdatedEvent` — new event type

### Changed Redux state shape
- `chatRuntimeSlice`: Added `taskBoardByThread: Record<string, TaskBoard>` — **no persist whitelist concern** (chatRuntime is not persisted per existing pattern in the slice)
- `clearRuntimeForThread` and `clearAllChatRuntime` updated to clear taskBoardByThread — **PASS**

### Changed types
- `PersistedTurnState`: Added optional `taskBoard?: TaskBoard | null` — backward compatible
- `ChatSendParams`: `model` changed from required `string` to optional `string` — this is a signature change but callers already pass the value. **Minor concern**: the `model` field was required before, now optional. The `chatSend` call in `Conversations.tsx` still passes it.
- `WebChannelEvent` (socketio.rs): Added `task_board: Option<serde_json::Value>` — backward compatible (skip_serializing_if None)
- `ChatStartPayload` (socketio.rs): Added `profile_id: Option<String>` with `#[serde(default)]` — backward compatible

### Changed RPC methods
- New: `openhuman.agent_profiles_list`, `openhuman.agent_profile_select`, `openhuman.agent_profile_upsert`, `openhuman.agent_profile_delete`
- New: `openhuman.threads_task_board_get`, `openhuman.threads_task_board_put`
- Modified: `openhuman.channel_web_chat` — added optional `profile_id` param. Schema updated, backward compatible.

## Full Diff

The complete diff is 6629 lines. Key sections by feature:

### Feature 1: Task Board (task_board.rs, todo_write.rs, TaskKanbanBoard.tsx)
- `src/openhuman/agent/task_board.rs` — 470 lines NEW: `TaskBoard`, `TaskBoardCard`, `TaskCardStatus` types, `TaskBoardStore` with get/put/delete, `normalise_board`, `validate_thread_id`, hex-encoded file naming
- `src/openhuman/tools/impl/agent/todo_write.rs` — Enhanced: `TodoStatus` gains `Blocked`/aliases, `TodoItem` gains `id`/`notes`/`blocker`, new `persist_thread_board` function, `TaskBoardPersistError` enum, progress emission via `AgentProgress::TaskBoardUpdated`
- `app/src/pages/conversations/components/TaskKanbanBoard.tsx` — 107 lines NEW: 4-column kanban (todo, in_progress, blocked, done), arrow move buttons, card notes/blocker display
- `app/src/types/turnState.ts` — `TaskBoardCard`, `TaskBoard`, `TaskBoardCardStatus` types + response types
- `src/openhuman/threads/schemas.rs` — `handle_task_board_get`, `handle_task_board_put` RPC handlers
- `src/openhuman/threads/turn_state/types.rs` — `task_board: Option<TaskBoard>` in TurnState
- `src/openhuman/threads/turn_state/mirror.rs` — observe TaskBoardUpdated, flush
- `app/src/store/chatRuntimeSlice.ts` — `taskBoardByThread` state, actions, hydration
- `app/src/services/chatService.ts` — `task_board_updated` socket event
- `app/src/providers/ChatRuntimeProvider.tsx` — `onTaskBoardUpdated` handler

### Feature 2: Parallel Agents (spawn_parallel_agents.rs)
- `src/openhuman/tools/impl/agent/spawn_parallel_agents.rs` — 735 lines NEW: `SpawnParallelAgentsTool`, `ParallelAgentTask`/`ParallelAgentResult` types, validation (min 2 tasks, max parallel, registry check, integrations_agent toolkit check), ownership boundaries, `join_all` fan-out, per-task progress/error events
- `src/openhuman/tools/ops.rs` — registered in tool list
- `src/openhuman/agent/agents/orchestrator/agent.toml` — added to orchestrator's named tools

### Feature 3: Agent Profiles (profiles.rs, agentProfilesApi.ts, Conversations.tsx)
- `src/openhuman/agent/profiles.rs` — 708 lines NEW: `AgentProfile`, `AgentProfilesState`, `AgentProfileStore` (load/save/select/upsert/delete/resolve), 4 built-in profiles (Default, Research, Planner, Review), `AgentProfilePromptSection`, normalisation/slugification
- `src/openhuman/agent/schemas.rs` — 4 RPC handlers (profiles_list, profile_select, profile_upsert, profile_delete)
- `src/openhuman/agent/harness/session/builder.rs` — `from_config_for_agent_with_profile` constructor, profile prompt section injection
- `src/openhuman/channels/providers/web.rs` — profile-aware session building: resolve profile, override model/temperature, set visible tools, routing by profile agent_id
- `app/src/types/agentProfile.ts` — NEW: TypeScript types
- `app/src/services/api/agentProfilesApi.ts` — NEW: RPC client
- `app/src/pages/Conversations.tsx` — profile selector dropdown, create form, state management

### Feature 4: E2E/CI Updates
- `.github/workflows/` — Appium install refactor (deterministic temp prefix, wrapper script), coverage timeout increase
- `app/scripts/e2e-build.sh` — E2E env vars
- `app/scripts/e2e-run-session.sh` — CEF sandbox conditional logic, workspace config dir
- `app/test/e2e/` — onboarding flow CTA updates, billing handoff assertions
- `scripts/mock-api/routes/integrations.mjs` — Composio toolkits + execute stubs
- Various test reliability improvements (wait_for_events, event filtering, dual lock guards)
