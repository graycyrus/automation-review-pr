# PR #2920 — fix(observability): demote "Config loading timed out" out of Sentry
- **Author**: @YellowSnnowmann
- **Branch**: fix/config-load-timeout-sentry → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2920
- **Status**: clean
- **Last reviewed commit**: 1c7f18bae11541b29cd81705a0a71f3d1dcb9de7
- **Last review date**: 2026-05-29T10:04:12Z

## AI Summary
- **What it does**: Adds `ExpectedErrorKind::ConfigLoadTimedOut` variant and `is_config_load_timed_out_message()` matcher to demote "Config loading timed out" errors (originating from `load_config_with_timeout` / `reload_config_snapshot_with_timeout` in `src/openhuman/config/ops.rs`) to `warn!` breadcrumbs instead of Sentry error events. Drops TAURI-RUST-5X (~318 events). Also removes verbose doc comment blocks from existing `ExpectedErrorKind` variants and `is_session_expired_message`.
- **Breaking risk**: Zero. No API, RPC contract, schema, or behavior change — only Sentry routing is affected. Error string returned to callers is unchanged.
- **Security risk**: Zero. Observability-internal change only.
- **Bottom line**: Safe to merge.

## Review History

### Review 1 — 2026-05-29T10:04:12Z
**Type**: Fresh
**Commit**: 1c7f18bae11541b29cd81705a0a71f3d1dcb9de7
**Summary**: Single-file change to `src/core/observability.rs`. Adds new `ConfigLoadTimedOut` enum variant, `is_config_load_timed_out_message()` matcher (substring on exact lowercase phrase), dispatch arm in `expected_error_kind()`, and `report_expected_message()` warn arm. Includes two new unit tests. Also removes substantial doc comment blocks from multiple existing variants and the session-expired matcher.
**Gates**: CI pending | Conflicts pass | Unresolved feedback pass
**Areas changed**: Rust core (observability)
**Red flags**: CI pending
**Linked issues**: TAURI-RUST-5X (Sentry, ~318 events) — no GitHub issue
**PR-Issue alignment**: Change is narrowly scoped to the stated Sentry issue; bundled doc deletions are adjacent scope
**CodeRabbit dedup**: CodeRabbit approved with no inline comments — nothing to skip
**Resolution actions**: None
**Surrounding code patterns checked**: Existing ExpectedErrorKind variants (DiskFull, LoopbackUnavailable patterns)
**Dependency audit**: N/A — no dependency changes
**Test coverage**: Two new tests — `classifies_config_load_timed_out` (canonical + RPC-wrapped shapes) and `does_not_classify_unrelated_timeouts_as_config_load_timed_out` (network/HTTP/cron negatives). Adequate coverage of changed lines.
**Impact scan**: N/A — internal observability routing only; `expected_error_kind` is an internal function, no public API changes
**Findings**:
- [minor] `src/core/observability.rs` — `ConfigLoadTimedOut` variant added without a doc comment despite PR description stating one was included and all other variants having `///` blocks documenting Sentry issue IDs, event counts, and emit sites
- [minor] `src/core/observability.rs` — bundled doc comment deletions from `LoopbackUnavailable`, `PromptInjectionBlocked`, `ContextWindowExceeded`, `UPDATER_TRANSIENT_MESSAGE_PHRASES`, and three `is_session_expired_message` arms remove Sentry issue cross-references (R5/R6, 140, 501, CD, 4P0, 4K5, 1EE) and conjunctive-anchor rationale from the codebase
**Action taken**: COMMENT — code is clean, CI pending; will approve once CI goes green
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2920#pullrequestreview-4388583767
