# PR #2086 — feat: add Novita AI provider
- **Author**: @Alex-yang00
- **Branch**: feat/novita-ai-provider → main
- **Created**: 2026-05-18
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2086
- **Status**: changes-requested
- **Last reviewed commit**: 67bc76520561b4fc8a565d990a094b1b8ff5904a
- **Last review date**: 2026-05-29T04:50:18Z

## Review History

### Review 1 — 2026-05-29T04:50:18Z
**Type**: Fresh
**Commit**: 67bc76520561b4fc8a565d990a094b1b8ff5904a
**Summary**: Adds Novita AI as a built-in cloud provider using the existing OpenAI-compatible path. Changes are confined to AIPanel.tsx and its test file — adds the novita chip, default endpoint, default model prefill (deepseek/deepseek-v4-pro), and routing dialog UX. 156 additions, 12 deletions across 2 files.
**Gates**: CI FAIL | Conflicts pass | Unresolved feedback pass
**Areas changed**: Frontend (settings panel, tests)
**Red flags**: CI failing — Prettier format check + own test fails
**Linked issues**: None
**PR-Issue alignment**: N/A — no linked issue
**CodeRabbit dedup**: CodeRabbit review was skipped (not run)
**Resolution actions**: None
**Surrounding code patterns checked**: AIPanel.tsx diff read in full
**Dependency audit**: N/A — no dependency changes
**Test coverage**: Tests added for Novita connect flow and routing defaults, but one test fails in CI
**Impact scan**: BUILTIN_CLOUD_SLUGS / CLOUD_EDITOR_SLUGS split reviewed — additive only, no callers broken

**Findings**:
- [major] app/src/components/settings/panels/__tests__/AIPanel.test.tsx:439 — `screen.getByDisplayValue('Custom')` matches both the slug `<select>` and the label `<input>` (both show "Custom" for the custom slug). Causes `TestingLibraryElementError: Found multiple elements` — this is the test failure in CI.
- [major] app/src/components/settings/panels/AIPanel.tsx:1637 — Prettier format check fails on the multiline ternary placeholder expression. Author ran `git diff --check` but not `pnpm --filter openhuman-app format`.
- [minor] BUILTIN_PROVIDER_DEFAULT_MODELS novita value `'deepseek/deepseek-v4-pro'` — should be verified as a live slug on Novita's model catalog before merge. If invalid, users hit errors on first use.

**Action taken**: COMMENT (CI failing + code has issues)
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2086#pullrequestreview-4386677456

## AI Summary
- **What it does**: Adds Novita AI as a new built-in cloud provider chip in the settings panel, using the existing OpenAI-compatible bearer auth flow with endpoint `https://api.novita.ai/openai`. Prefills `deepseek/deepseek-v4-pro` as the default routing model for Novita. Extends the custom routing dialog to auto-set default models when switching providers.
- **Breaking risk**: Low — purely additive. Existing providers and their configs are untouched. The BUILTIN_CLOUD_SLUGS refactor correctly separates chip providers from editor providers.
- **Security risk**: Low — no new auth surfaces. Uses existing bearer token pattern. HTTPS endpoint. No secrets, no injection vectors.
- **Bottom line**: Safe to merge — once CI is fixed.
