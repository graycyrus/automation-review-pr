# PR #3545 — feat(chat): make reasoning-v1 vision-capable; remove pro-reasoning tier

## Metadata
| Field | Value |
|-------|-------|
| Author | @sanil-23 |
| Branch | `feat/remove-pro-reasoning` → `main` |
| Created | 2026-06-09 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3545 |
| Status | changes-requested |
| Last reviewed commit | bd03b48bf8db5e92428b5dc79dcf0de76c3c8c58 |
| Last review date | 2026-06-09T12:24:44Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Moves vision capability from the removed `pro-reasoning-v1` tier onto `reasoning-v1`, removes the `pro-reasoning-v1` tier entirely (const, pricing, routing, profile, context window), removes the Pro-reasoning chat-header toggle, and updates advisory copy across 14 locales. Product simplification: one reasoning tier instead of two, with vision on the survivor. |
| Breaking risk | Medium | `pro-reasoning-v1` and `hint:pro-reasoning` are removed from the known-tier registry; persisted configs with those values fall back to `reasoning-v1` / default profile rather than erroring. The fallback is documented and tested, but clients relying on the removed profile ID will silently downgrade. |
| Security risk | Zero | No auth, secrets, injection, or access-control changes. |
| Bottom line | Not safe — the client-side vision flag for `reasoning-v1` is live, but backend vision support is an explicitly open question; users attaching images on the Reasoning tier could hit runtime errors. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Code is clean, well-reasoned, and internally consistent. PR description is detailed and honest about open questions. |
| Structural signals | None | No over-abstraction, redundant comments, or boilerplate. |
| Content signals | None | Naming is domain-appropriate throughout. |
| Behavioral signals | None | Tests were updated alongside the feature changes. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | The Pro-reasoning radio button is removed from the chat-header profile toggle. The Reasoning tier now accepts image attachments. |
| User flow | Modified — users who previously switched to Pro-reasoning for vision must now use the Reasoning tier directly. The advisory copy in all 14 locales now points at "OpenHuman Reasoning tier". |
| Affected surfaces | `Conversations.tsx` chat header (profile toggle group); image attachment advisory toast/copy. |
| Responsiveness | Not applicable — button removal; no layout risk. |
| Accessibility | The Pro-reasoning `role="radio"` button is removed; group still has Quick + Reasoning buttons with correct `aria-checked`. No regression. |
| i18n | 14 locale files updated consistently — `chat.agentProfile.proReasoning` removed, `chat.attachment.imageNotSupported` reworded. Checked. |
| Performance | Not applicable. |
| Edge states | Stale persisted `pro-reasoning` profile ID handled by `normalise_state` (falls back to default profile). Documented and tested. |
| Risk | Medium |
| Recommendation | Needs confirmation of backend vision readiness before approving. |

## Review History

### Review 1 — 2026-06-09T12:24:44Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | bd03b48bf8db5e92428b5dc79dcf0de76c3c8c58 |
| Gates | CI fail (E2E lane 2/4 failing; Frontend Coverage + Rust Core Coverage pending) \| Conflicts pass \| Feedback pass |
| Areas changed | Rust core (factory, profiles, cost, types, model_context), Frontend (Conversations.tsx, 14 i18n files), Tests (factory_tests.rs, model_context.rs, web_tests.rs, Conversations.attachments.test.tsx) |
| Linked issues | None |
| PR-Issue alignment | N/A |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Removes `pro-reasoning-v1` tier comprehensively — const, pricing, routing hints, always-managed role guard, built-in profile, context window match arm, and all factory/session references — while moving vision capability to `reasoning-v1`. Frontend removes the Pro-reasoning toggle and updates advisory copy in 14 locales. |
| CodeRabbit dedup | No CodeRabbit inline comments on this PR. |
| Resolution actions | None |
| Surrounding code checked | Diff is self-contained; `make_openhuman_backend` fallback path described in PR body (not shown in diff but PR author confirms it). |
| Dependency audit | N/A — no new dependencies. |
| Test coverage | Tests updated correctly: new `reasoning_is_the_vision_capable_managed_tier` test, removed `pro_reasoning_*` tests, updated attachment test to match Reasoning tier behavior. Coverage jobs still pending. |
| Impact scan | All `pro-reasoning-v1` and `hint:pro-reasoning` references removed across the codebase as shown in diff. |

#### Findings
- [major] `src/openhuman/inference/provider/factory.rs` (`oh_tier_supports_vision`) + `model_context.rs` — Client-side vision flag for `reasoning-v1` is now live, but the PR body explicitly acknowledges backend vision readiness is an open question. If `api.tinyhumans.ai` doesn't route `reasoning-v1` to a vision-capable provider, users attaching images on the Reasoning tier will get runtime errors. Needs backend confirmation or a graceful error path before merging.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — CI failing (E2E lane 2/4) + major concern about unconfirmed backend vision readiness for `reasoning-v1`. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3545#pullrequestreview-4458469598 |
