# PR #3016 — feat(tools): add generate_presentation tool (native rust engine, ppt-rs) (#2778)

## Metadata
| Field | Value |
|-------|-------|
| Author | @oxoxDev |
| Branch | feat/2778-presentation-tool → main |
| Created | 2026-05-30 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3016 |
| Status | changes-requested |
| Last reviewed commit | c9d6c551c9f22487613df4b58b018f17f1aac1af |
| Last review date | 2026-06-01T16:15:36Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Adds a `generate_presentation` agent tool that converts a structured slide spec into a `.pptx` file via a pure-Rust `ppt-rs` engine. Also ships `artifacts::store` producer helpers (`create_artifact`, `finalize_artifact`, `fail_artifact`) and `runtime_python::venv`/`run` scaffolding for future Python-backed tools. |
| Breaking risk | Low | `ArtifactMeta.error` is additive with `#[serde(default)]`; existing on-disk records parse unchanged. No changes to public RPCs or exported types. |
| Security risk | Zero | No subprocess, no pip install at runtime, no network access from the tool. Slide content stays in-process and lands in the workspace artifacts directory. |
| Bottom line | Safe to merge once CI passes and open issues resolved. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Code is well-structured, tests cover meaningful branches, no excessive comments on obvious logic. PR description is detailed but appropriate for a multi-file feature. |
| Structural signals | None | Clean module layout, reasonable abstractions. |
| Content signals | None | Variable names are domain-specific, no generic placeholders. |
| Behavioral signals | None | 15 unit tests + 4 engine tests for a 2186-line addition is appropriate coverage. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — no frontend changes in this PR. ArtifactCard UI lands in PR #3017. |
| User flow | Unchanged — the agent now has a working `generate_presentation` tool but the UI rendering is a follow-up PR. |
| Affected surfaces | N/A |
| Responsiveness | Not applicable |
| Accessibility | Not applicable |
| i18n | Not applicable |
| Performance | Not applicable |
| Edge states | Not applicable |
| Risk | None |
| Recommendation | Approve as-is once CI clean and code issues resolved |

## Review History

### Review 1 — 2026-06-01T16:15:36Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | c9d6c551c9f22487613df4b58b018f17f1aac1af |
| Gates | CI fail (Rust Core Coverage + E2E lane 1/4) \| Conflicts pass \| Feedback pass |
| Areas changed | Rust core (artifacts/store, runtime_python, tools/impl/presentation), CI config, test helpers |
| Linked issues | #2778 |
| PR-Issue alignment | PR pivots from python-pptx to ppt-rs (native Rust) compared to issue #2778's acceptance criteria, but spirit of the issue is met and the pivot is explicitly documented. Not blocking. |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds `generate_presentation` tool backed by ppt-rs native Rust engine. Also adds artifact lifecycle helpers (`create_artifact`/`finalize_artifact`/`fail_artifact`) and `runtime_python::venv`+`run` scaffolding (unused by this tool, shipped for future Python-backed tools). Registration in `tools/ops.rs` and `orchestrator/agent.toml`. |
| CodeRabbit dedup | All previously-flagged major findings (stdin deadline, venv log redaction, venv timeout, generate_pptx.py path check, PII in logs, artifact-not-failed on error, deterministic temp paths, theme length validation, test preflight) have been addressed by @oxoxDev per CodeRabbit confirmation comments. Three remain open (engine.rs:130 spawn_blocking, mod.rs finalize_artifact, engine.rs:229 minor). Did not repeat any of these in review. |
| Resolution actions | None — continuation review |
| Surrounding code checked | Reviewed engine.rs generate() call structure and spawn_blocking timeout pattern. Checked coverage.yml context around the added step. |
| Dependency audit | `ppt-rs 0.2.14` (Apache-2.0, crates.io) — pulls in `pulldown-cmark`, `syntect`, `clap`, `zip 0.6.6`, `xml-rs`. All active, permissive licenses. ~10-15 MB binary bloat acknowledged in PR. No supply chain red flags. |
| Test coverage | 15 unit tests in tests.rs + 4 engine tests in engine.rs. Coverage CI is currently failing so the 80% gate is unverified. |
| Impact scan | `ArtifactMeta` field `error` is additive with serde default. `create_artifact`/`finalize_artifact`/`fail_artifact` are new exports. Tool registration is always-on (no feature gate). Downstream PRs #3017, #3026, #3029 are stacked on top and unaffected by the engine swap per PR description. |

#### Findings
- [major] `.github/workflows/coverage.yml:96` — stale `pip install python-pptx==1.0.2` step references the old python-pptx engine; almost certainly the cause of "Rust Core Coverage" CI failure. Remove or update to match the native-Rust path.
- [minor] `src/openhuman/agent_registry/agents/orchestrator/agent.toml:168` — comment says "via a bundled python-pptx helper" — stale after engine refactor, should say ppt-rs / native Rust.
- [major] `engine.rs:130` (CodeRabbit open) — `tokio::time::timeout` wrapping `spawn_blocking` doesn't cancel the blocking thread. Leaked thread pool slot on timeout.
- [major] `presentation/mod.rs` (CodeRabbit open) — `finalize_artifact` failure via `?` leaves artifact stuck in `Pending` state.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — CI failing (Rust Core Coverage + E2E lane 1/4) and 2 open CodeRabbit major findings + stale CI step. Listed all blocking items for @oxoxDev. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3016#pullrequestreview-4402825543 |
