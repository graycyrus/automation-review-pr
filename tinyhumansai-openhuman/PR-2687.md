# PR #2687 — feat(intelligence): add architecture diagram viewer
- **Author**: @sunilkumarvalmiki
- **Branch**: `codex/OH-1854-diagram-viewer` → `main`
- **Created**: 2026-05-26
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2687
- **Status**: clean
- **Last reviewed commit**: 0fdf860350d42a336c2ebd4a1564b878111f4093
- **Last review date**: 2026-05-28T18:27:07Z

## AI Summary

**What it does**: Adds a Diagram tab to the Intelligence dashboard that loads architecture diagrams from a configurable local endpoint (default `http://localhost:8787/workspace/diagrams/latest.png`), with auto-refresh on a configurable interval and manual refresh. Adds `[dashboard.diagram_viewer]` config schema in Rust with defaults and exposes a new read-only `openhuman.config_get_dashboard_settings` RPC controller so the frontend can read config. Localized across 13 locales.

**Breaking risk**: Low. All new config fields have serde defaults, no existing APIs changed, no DB schema changes, no exports removed.

**Security risk**: Low. The diagram viewer loads from an operator-configured localhost URL; no injection surface, no secrets, no auth bypass.

**Bottom line**: Safe to merge — pending CI fix (one failing parity test).

## Review History

### Review 1 — 2026-05-28T18:27:07Z
**Type**: Fresh
**Commit**: 0fdf860350d42a336c2ebd4a1564b878111f4093
**Summary**: Adds DiagramViewerTab component with cache-busting refresh logic, wires it into Intelligence.tsx as a new tab, adds DiagramViewerConfig/DashboardConfig Rust schema structs, registers the get_dashboard_settings controller with extensive structured logging, adds legacy alias parity on both frontend and server, and covers all new behavior with Vitest + cargo tests.
**Gates**: CI FAILING | Conflicts pass | Prior graycyrus review dismissed (no open threads)
**Areas changed**: Frontend (Intelligence tab, new component), Rust config schema/ops/schemas/legacy_aliases, i18n (13 locales)
**Red flags**: CI failing on `frontend_legacy_aliases_match_server_alias_table` parity test
**Linked issues**: Closes #1854
**PR-Issue alignment**: All 8 acceptance criteria from #1854 are met — diagram tab, image loads, auto-refresh, manual refresh, empty state, full-width display, config-driven, graceful error handling.
**CodeRabbit dedup**: Both CodeRabbit findings (i18n strings, diagnostics logging) were already addressed in commits b616f48–0519fab. Not repeated.
**Resolution actions**: None needed for prior threads — all dismissed.
**Surrounding code patterns checked**: Compared against `openhumanGetSearchSettings` and `openhumanGetAnalyticsSettings` patterns in config.ts — consistent. Compared DiagramViewerConfig against ModelHealthConfig in dashboard.rs — consistent.
**Dependency audit**: No new external dependencies introduced.
**Test coverage**: Frontend: 2 test files, 5 Vitest tests covering happy path, error state, tab integration, and buildDiagramImageUrl edge cases. Rust: 3 new tests (dashboard defaults, partial TOML deserialize, summarization hint for unrelated factory). Coverage gate skipped (depends on failing Rust coverage run).
**Impact scan**: `configGetDashboardSettings` is new — no existing callers. `DiagramViewerConfig` exported from mod.rs — no callers outside of dashboard.rs. `openhumanGetDashboardSettings` only called in DiagramViewerTab.
**Findings**:
- [minor] `app/src/components/intelligence/DiagramViewerTab.tsx:148` — Manual `.replace('{seconds}', ...)` on i18n string is fragile if placeholder format drifts across locales
**Action taken**: COMMENT — code is clean, CI failing on `frontend_legacy_aliases_match_server_alias_table` parity test (likely main drift, not a PR bug). Will approve once CI is green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2687#pullrequestreview-4383628379
