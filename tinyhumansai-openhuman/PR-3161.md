# PR #3161 — fix(inference): normalize 0.0.0.0 to 127.0.0.1 in Ollama URL + add endpoint edit button

## Metadata
| Field | Value |
|-------|-------|
| Author | @senamakel-droid |
| Branch | `issue/3087-no-ui-field-to-set-custom-ollama-host-po` → `main` |
| Created | 2026-05-30 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3161 |
| Status | changes-requested |
| Last reviewed commit | 51ec889b3bacba86f1ae00f7a087e928b6f68ac0 |
| Last review date | 2026-06-01T18:03:56Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Fixes Ollama's default `OLLAMA_HOST=0.0.0.0:11434` being used as a client address on Windows (invalid; rewrites to `127.0.0.1`). Also adds a pencil edit button on enabled local-runtime chips in Settings → AI so users can change the endpoint URL without toggling the provider. Adds i18n key across 14 locales. |
| Breaking risk | Low | `LocalAiSettingsPatch.base_url` changed from `Option<String>` to `Option<Option<String>>` in Rust — all callers in the PR are updated, serialization layer handles the three-way null/absent/string split correctly via `deserialize_present_json`. |
| Security risk | Zero | No auth, secrets, injection surfaces touched. Ollama URL rewriting is purely host normalization. |
| Bottom line | Safe to merge | Core logic is correct; one frontend IPv6 normalization bug needs fixing before merge. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Clean PR — tight scope, real tests (10 Rust unit tests, 3 frontend tests added), no excessive comments on obvious code, no dead code beyond the IPv6 bug. |
| Structural signals | None | — |
| Content signals | None | — |
| Behavioral signals | None | 1018 additions across ~55 files is large but justified: i18n covers 14 locales (28 lines), test files cover new logic, e2e files updated for API shape change. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | Enabled Ollama/LM Studio chips in Settings → AI gain a pencil icon button on the right. |
| User flow | Modified — users can now change the Ollama server endpoint without disabling/re-enabling the provider. |
| Affected surfaces | Settings → AI panel (provider chips), `ProviderKeyDialog` (pre-populated with existing endpoint) |
| Responsiveness | Not applicable — settings panel, no breakpoint concerns. |
| Accessibility | Checked — edit button has `aria-label` and `title` set via i18n key. |
| i18n | All 14 locale files updated with `settings.ai.editEndpoint` key and real translations. |
| Performance | Not applicable. |
| Edge states | Checked — button only shown when `enabled === true`, dialog pre-populated via `initialValue` prop. |
| Risk | Low |
| Recommendation | Approve as-is after CI fix |

## Review History

### Review 1 — 2026-06-01T18:03:56Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | 51ec889b3bacba86f1ae00f7a087e928b6f68ac0 |
| Gates | CI fail \| Conflicts pass \| Feedback pass |
| Areas changed | Rust core (ollama.rs, config/ops.rs, compatible.rs), Frontend (AIPanel.tsx, aiSettingsApi.ts, ollamaUrlValidation.ts, Conversations.tsx, OAuthProviderButton.tsx), i18n (14 locales), Tests |
| Linked issues | #3087 |
| PR-Issue alignment | Full match — issue asked for (1) UI field to change Ollama endpoint and (2) fix for 0.0.0.0 client-connect bug. Both delivered. |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds `normalize_unspecified_host()` in `ollama.rs` that rewrites `0.0.0.0`/`[::]` to `127.0.0.1`/`[::1]` before any HTTP call. Adds pencil edit button to enabled local-runtime chips; `ProviderKeyDialog` gets `initialValue` prop to pre-populate. `LocalAiSettingsPatch.base_url` changed to `Option<Option<String>>` to express null-clear semantics distinctly from absent. `ollama` removed from the `cloudProviders` filter exclusion list so the endpoint value is available in the draft for pre-population. |
| CodeRabbit dedup | Skipped: (1) `ollama.rs:21` path dropped in host rebuild, (2) `ollama.rs:25` raw URL logged without redaction, (3) `compatible.rs:574` reasoning_content stripped on pruned tool-call collapse, (4) `OAuthProviderButton.tsx:32` isLoading timeout vs loopback listener lifetime, (5) `ko.ts:2791` Korean translation. All already flagged by CodeRabbit. |
| Resolution actions | None — fresh review. |
| Surrounding code checked | `app/src/services/api/aiSettingsApi.ts` (cloudProviders filter change), `src/openhuman/config/schemas.rs` (serialization layer for base_url Option<Option<String>>). |
| Dependency audit | N/A — no new dependencies. |
| Test coverage | Good. 10 new Rust unit tests for normalization, 3 new frontend tests for edit button visibility and pre-population. One gap: no frontend test for IPv6 `[::]` normalization. |
| Impact scan | `LocalAiSettingsPatch.base_url` type change — all call sites in the PR updated (ops.rs, ops_tests.rs, schemas.rs). Serialization handled via `deserialize_present_json` + three-way match in `handle_update_local_ai_settings`. |

#### Findings
- **[major]** `app/src/utils/ollamaUrlValidation.ts:44-47` — `parsed.hostname === '[::]'` is dead code. JS `URL.hostname` strips brackets from IPv6 addresses; `new URL('http://[::]:11434').hostname` returns `'::'` not `'[::]'`. IPv6 all-interfaces addresses pass through unnormalized. Fix: `parsed.hostname === '::'`, and add a test for `http://[::]:11434`.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — CI failing (Vitest, Rust E2E, Rust Core Coverage, Playwright lane 1, PR Submission Checklist); also one major frontend IPv6 normalization bug. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3161#pullrequestreview-4403526195 |
