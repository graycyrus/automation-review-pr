# PR #3138 — feat(memory-sync): track Composio API action count + cost in sync audit (#3111)
- **Author**: @justinhsu1477
- **Branch**: feat/composio-audit-cost → main
- **Created**: 2026-05-30
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3138
- **Status**: changes-requested
- **Last reviewed commit**: 3032cfefb09a15b9cc38aa58b8b0a2818db7ecff
- **Last review date**: 2026-06-01T11:54:24Z

## Review History

### Review 1 — 2026-06-01T11:54:24Z
**Type**: Fresh
**Commit**: 3032cfefb09a15b9cc38aa58b8b0a2818db7ecff
**Summary**: Adds Composio API action tracking to the sync audit log. Introduces `ComposioUsage` / `ComposioUsageHandle` (`Arc<Mutex<ComposioUsage>>`), wires it onto `ProviderContext`, tallies at the `execute` chokepoint, threads the tally back through `run_connection_sync`, and writes `composio_actions_called` + `composio_cost_usd` into `SyncAuditEntry`. Adds `combined_cost_usd()` helper and `#[serde(default)]` back-compat. Also adds `usage: Default::default()` to all other `ProviderContext` construction sites (non-audit callers; harmless).
**Gates**: CI FAILING (Rust E2E mock backend, Rust Core Coverage, Frontend Coverage, Playwright lane 1) | Conflicts: pass | Unresolved feedback: CodeRabbit changes-requested (failure path bug)
**Areas changed**: Rust core (memory_sync, memory_sources, composio ops, task_sources, bin/slack_backfill), tests
**Red flags**: CI failing on 4 checks; CodeRabbit active changes-requested
**Linked issues**: #3111
**PR-Issue alignment**: AC #1 (action count) and AC #2 (actual cost from API response) met. AC #3 (UI panel) partially met — `combined_cost_usd()` backend helper exposed, UI wiring explicitly deferred to follow-up. Not BLOCKING.
**CodeRabbit dedup**: CodeRabbit flagged [major] failure-path usage loss in `run_connection_sync` — `provider.sync().await?` exits before `ctx.usage` is read, zeroing Composio cost in failure-path audit entries despite PR claiming otherwise. Valid finding, not repeated in review body.
**Resolution actions**: None
**Surrounding code patterns checked**: `mod.rs`, `types.rs`, `memory_sources/sync.rs`, `audit.rs`
**Dependency audit**: N/A (no new deps)
**Test coverage**: 4 new unit tests: shared-handle contract, default-zero, combined_cost helper, legacy-line back-compat deserialization. Adequate for the changed logic.
**Impact scan**: `run_connection_sync` return type changed from `Result<SyncOutcome>` to `Result<(SyncOutcome, ComposioUsage)>` — all callers updated. `ProviderContext` gains `usage` field — all construction sites updated with `Default::default()`. No unhandled breakage found.
**Findings**:
- [major] `src/openhuman/memory_sync/composio/mod.rs:209` — failure path loses usage (CodeRabbit, not repeated in review)
- [minor] `src/openhuman/memory_sync/composio/periodic.rs:321` — `run_one_tick` adds `usage: Default::default()` but may never read it back if periodic path calls `provider.sync()` directly; periodic sync audit entries likely miss Composio cost data
**Action taken**: COMMENT — CI failing, referenced CodeRabbit finding as valid, flagged periodic sync coverage gap
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3138#pullrequestreview-4400919858

## AI Summary
**What it does**: Adds two new fields (`composio_actions_called`, `composio_cost_usd`) to the sync audit log by hooking into the single `ProviderContext::execute` chokepoint. The tally is accumulated in a shared `Arc<Mutex<ComposioUsage>>` and read back by `run_connection_sync` after the provider completes. Legacy audit JSONL lines remain parseable via `#[serde(default)]`.
**Breaking risk**: Low. `run_connection_sync` return type changes to a tuple but is crate-internal; all callers updated. `ProviderContext` struct gains a field; all construction sites patched.
**Security risk**: Zero. Pure telemetry — no user-facing surfaces, no secrets, no injection vectors.
**Bottom line**: Safe to merge once CI is green and the failure-path usage bug is fixed.

## UI Impact
- **Visual change**: None — backend Rust only, no frontend files changed
- **User flow**: Unchanged
- **Affected surfaces**: None (UI wiring deferred to follow-up)
- **Risk**: None
- **Recommendation**: N/A
