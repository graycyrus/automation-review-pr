# PR #3274 — feat(artifacts): persist thread_id + ai_list_artifacts thread filter (#3226)

## Metadata
| Field | Value |
|-------|-------|
| Author | @YOMXXX |
| Branch | feat/artifact-thread-id → main |
| Created | 2026-06-03 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3274 |
| Status | changes-requested |
| Last reviewed commit | abd518e3030f8bb411aae8e29921b09eba27bc0d |
| Last review date | 2026-06-03T13:19:45Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Adds `thread_id: Option<String>` to `ArtifactMeta` (populated at create-time from `APPROVAL_CHAT_CONTEXT`) and a `thread_id` filter to `ai_list_artifacts`, so `ChatFilesPanel` can repopulate from disk after a redux-persist purge or fresh-device boot. This is the backend half of #3226; frontend wiring is deferred. |
| Breaking risk | Low | `list_artifacts` gains a 4th arg (`thread_id: Option<&str>`) — all internal callers are updated in the same PR. The RPC schema change is additive (new optional field). Serde backward compat is handled correctly with `#[serde(default, skip_serializing_if = "Option::is_none")]`. |
| Security risk | Zero | `thread_id` filter is an equality comparison on a string field — no injection surface. `read_optional_string` validates type and trims whitespace safely. No secrets or PII in logs beyond the thread_id debug line, which is acceptable. |
| Bottom line | Safe to merge | Code is correct; only blocker is the `Closes #3226` auto-close issue reference that would orphan the unmet frontend AC. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | Code follows existing module patterns. Comments explain non-obvious invariants (filter-before-pagination, legacy exclusion semantics). Variable names are idiomatic Rust. Tests are behavior-focused and pin specific scenarios rather than covering trivially. |
| Structural signals | None | No over-abstraction, no unnecessary wrappers. `read_optional_string` is a justified helper (four distinct input states to handle). |
| Content signals | None | No generic names, no placeholder TODOs, no dead code. |
| Behavioral signals | None | PR is narrowly scoped to Rust core. Six tests added alongside logic changes. Style consistent with surrounding code. |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | None — no frontend changes in this PR. |
| User flow | Unchanged — `ChatFilesPanel` frontend wiring is a separate follow-up. |
| Affected surfaces | N/A |
| Responsiveness | Not applicable |
| Accessibility | Not applicable |
| i18n | Not applicable |
| Performance | Not applicable — filter is a linear scan over the in-memory artifact list, acceptable given typical artifact counts. |
| Edge states | Not applicable |
| Risk | None |
| Recommendation | N/A — backend-only PR |

## Review History

### Review 1 — 2026-06-03T13:19:45Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | abd518e3030f8bb411aae8e29921b09eba27bc0d |
| Gates | CI pass | Conflicts pass | Feedback pass |
| Areas changed | Rust core (artifacts module: ops, store, schemas, types, tools, tests) |
| Linked issues | #3226 |
| PR-Issue alignment | Backend ACs met (thread_id field, filter, legacy handling, tests, coverage gate). Frontend AC (ChatFilesPanel wiring) explicitly deferred — but `Closes #3226` in PR body will auto-close the issue on merge, orphaning that AC without a tracking home. |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Adds `thread_id` persistence to `ArtifactMeta` and a per-thread filter to `ai_list_artifacts`. Filter is correctly applied before pagination so `total` reflects per-thread count. `read_optional_string` helper handles absent/null/whitespace/wrong-type inputs. `ArtifactListTool` correctly passes `None` to keep agent tool surface unfiltered. |
| CodeRabbit dedup | CodeRabbit approved with empty body — no inline findings to dedup. |
| Resolution actions | None |
| Surrounding code checked | types.rs sibling fields for serde pattern conformance — consistent. |
| Dependency audit | N/A — no new dependencies. |
| Test coverage | 6 new tests: 3 types round-trip (`legacy_deserialises`, `none_skipped_in_serialised_form`, `some_round_trips`) + 3 ops filter scenarios (`without_filter_returns_all`, `with_filter_returns_only_matching`, `unknown_thread_returns_zero`). Coverage gate passed (diff-cover ≥ 80%). |
| Impact scan | `list_artifacts` caller in `store_tests.rs` updated in-PR. `ai_list_artifacts` caller in `tools.rs` updated in-PR. All call sites accounted for. |

#### Findings
- [major] PR body — `Closes #3226` will trigger GitHub auto-close on merge. Issue #3226 has an unmet frontend acceptance criterion (`ChatFilesPanel` calling `ai_list_artifacts` on mount) that is explicitly deferred. Fix: change to `Contributes to #3226` and open a follow-up issue for the frontend wiring, or keep #3226 open manually after merge.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | REQUEST_CHANGES — one process-level blocker (issue reference auto-close orphaning frontend AC) |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3274#pullrequestreview-4418882210 |
