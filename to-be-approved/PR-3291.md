# PR #3291 — fix(memory-graph): render large graphs in the SVG fallback without freezing

## Metadata
| Field | Value |
|-------|-------|
| Author | @sanil-23 |
| Branch | fix/memory-graph-svg-worker-layout → main |
| Created | 2026-06-03 |
| URL | https://github.com/tinyhumansai/openhuman/pull/3291 |
| Status | clean |
| Last reviewed commit | 4e0af1e7ca21807ae8c115a4b5e3e1be5a51593e |
| Last review date | 2026-06-03T10:53:21Z |

## AI Summary
| Dimension | Rating | Detail |
|-----------|--------|--------|
| What it does | — | Moves the SVG fallback's O(n²) synchronous force layout off the main thread into a Web Worker that streams `Float32Array` positions back per tick. Adds deterministic carry-over seeding so live data updates don't reshuffle surviving nodes, and adds progressive per-frame DOM mounting so a large graph never blocks React reconciliation. |
| Breaking risk | Zero | Desktop-only SVG renderer change. No shared types, no exported APIs, no Rust/Tauri/WebGL path touched. |
| Security risk | Zero | All communication is structured messages between main thread and worker. No eval, no dynamic imports from user data, no network calls, no secrets. |
| Bottom line | Safe | Safe to merge once CI is green. |

## AI Quality
| Signal | Detected | Detail |
|--------|----------|--------|
| AI slop | No | "AI Authored PR Metadata" section is explicit and honest. Implementation is sound — performance measurements cited, carry-over logic correct, tests cover edge cases. Over-documented in places (verbose JSDoc on private logic) but not harmful. |
| Structural signals | Over-documentation | Module-level JSDoc blocks on every new file; comment `// one integration step per frame` after `sim.tick()` is obvious. Not blocking. |
| Content signals | None | Variable names are meaningful and domain-specific. |
| Behavioral signals | None | Tests are present and substantive (15 seedSvgLayout cases, worker stub tests, MemoryGraph integration tests). |

## UI Impact
| Dimension | Value |
|-----------|-------|
| Visual change | SVG fallback memory graph now renders progressively (nodes appear in batches) and settles with auto-fit-to-viewport. Previously it froze/hung on large graphs; now it remains responsive. |
| User flow | Modified — Intelligence page no longer hangs on WebGL-unavailable devices (blocklisted GPU, --disable-gpu, headless). Reset View re-frames the graph instead of resetting to origin. |
| Affected surfaces | Intelligence > Memory Graph (SVG fallback path only; WebGL/Pixi path unchanged) |
| Responsiveness | Not applicable — desktop renderer |
| Accessibility | Not applicable — SVG graph, no interactive ARIA changes |
| i18n | Not applicable — no text changes |
| Performance | Significantly improved: main-thread block reduced from ~673ms (1k nodes) / hang (10k) to ~75ms / no crash |
| Edge states | Empty graph handled (returns null sim). Worker unavailable (jsdom) falls back to synchronous relaxLayout. |
| Risk | Low |
| Recommendation | Approve as-is after CI passes |

## Review History

### Review 1 — 2026-06-03T10:53:21Z

#### Context
| Field | Value |
|-------|-------|
| Type | Fresh |
| Commit | 4e0af1e7ca21807ae8c115a4b5e3e1be5a51593e |
| Gates | CI pending \| Conflicts pass \| Feedback pass |
| Areas changed | Frontend — intelligence/MemoryGraph.tsx, new files: svgForceLayout.worker.ts, useSvgForceLayout.ts, seedSvgLayout.ts + tests |
| Linked issues | None |
| PR-Issue alignment | N/A — no tracking issue linked |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | Replaces synchronous O(n²) main-thread relaxLayout with a d3-force Web Worker, adds deterministic position carry-over seeding, and wires up imperative DOM writes + progressive mounting in MemoryGraph. SVG 1k nodes: 673ms → 75ms; 10k nodes: hang → stable. |
| CodeRabbit dedup | Two majors flagged by CodeRabbit (stale position carry-over on sim swap; alive guard not invalidated in stop()) were both addressed in bbaef13. Skipped. |
| Resolution actions | None — fresh review |
| Surrounding code checked | memoryGraphLayout.ts (imports), MemoryGraph.tsx existing drag/pan/zoom handlers |
| Dependency audit | Uses existing d3-force (already in project). No new external dependencies. |
| Test coverage | Comprehensive: 15 seedSvgLayout unit tests (all paths), svgForceLayout.worker tests (tick/end/stop/drag/out-of-range), MemoryGraph.worker integration tests (worker wiring, progressive mount, drag-freeze, reheat, reset). jsdom-untestable paths (real Worker/rAF/fit) verified live over CDP per PR description. |
| Impact scan | No shared exports changed. PixiGraph path unchanged. SVG path is internal to MemoryGraph. |

#### Findings
- [minor] `useSvgForceLayout.ts:98` — `drag` callback is dead API: MemoryGraph only uses `svgLayout.stop`, never `svgLayout.drag`. The worker's drag-while-pinned path is unreachable from production. Wire it into node drag handlers or remove it from the return type.
- [minor] `MemoryGraph.tsx` — `FIRST_BATCH = 800` declared inside component body; should be a module-level constant alongside `NO_NODES`/`NO_RADII`/`SVG_CENTER`.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — code clean, CI pending; will approve once CI passes |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3291#pullrequestreview-4417813902 |
