# PR #2887 — feat(composio): migrate Notion provider to memory_tree pipeline (#2885)
- **Author**: @justinhsu1477
- **Branch**: feat/composio-notion-memory-tree → main
- **Created**: 2026-05-29
- **URL**: https://github.com/tinyhumansai/openhuman/pull/2887
- **Status**: clean
- **Last reviewed commit**: 8492dfbf4e14e648d55a0487d990499e743d7328
- **Last review date**: 2026-05-29T02:21:10Z

## Review History

### Review 1 — 2026-05-29T02:21:10Z
**Type**: Fresh
**Commit**: 8492dfbf4e14e648d55a0487d990499e743d7328
**Summary**: Migrates the Notion Composio provider off the legacy `UnifiedMemory` (`memory_docs`) ingest path onto the canonical memory-tree pipeline. Adds `providers/notion/ingest.rs` with `ingest_page_into_memory_tree`, updates `provider.rs` to call it instead of `persist_single_item`, and ships 5 targeted tests. 401 additions, 12 deletions across 3 files all within `src/openhuman/memory_sync/composio/providers/notion/`.
**Gates**: CI FAILING (PR Submission Checklist fail, multiple jobs pending) | Conflicts pass
**Areas changed**: Rust core — memory_sync/composio/providers/notion
**Red flags**: None
**Linked issues**: #2885 (feat: migrate Notion/ClickUp/Linear/GitHub Composio providers off UnifiedMemory)
**PR-Issue alignment**: Full alignment. Issue asks for exactly this: add `ingest.rs`, route through `ingest_pipeline::ingest_document` with stable `source_id = "notion:{connection_id}:{page_id}"`, regression test pinning `mem_tree_chunks` + `mem_tree_ingested_sources`. All acceptance criteria met.
**CodeRabbit dedup**: No CodeRabbit inline comments on this PR — nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: Diff only; Slack/Gmail ingest module pattern confirmed via PR description cross-reference.
**Dependency audit**: N/A — no new dependencies
**Test coverage**: 5 new tests in `ingest::tests`: regression test (`ingest_page_writes_to_memory_tree`), source_id stability (`notion_source_id_is_stable_and_namespaced`), timestamp parsing (`parse_edited_time_handles_valid_and_invalid_inputs`), body shape (`render_page_body_includes_title_header_and_pretty_json`), delete-replace round-trip (`re_ingesting_edited_page_replaces_prior_chunks`). All the right paths covered.
**Impact scan**: `persist_single_item` removed from Notion call site only — function itself stays for other callers (#2585). No exported type changes. Source_id format change is internal dedup-key only.
**Findings**: None
**Action taken**: COMMENT — CI failing/pending; code is clean. Posted walkthrough + AI risk assessment. Will approve once CI is green.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/2887#pullrequestreview-4386150945

## AI Summary
- **What it does**: Routes Notion sync through `ingest_pipeline::ingest_document` so pages land in `mem_tree_chunks` + `mem_tree_ingested_sources` instead of the legacy `memory_docs` table. Fixes a silent-failure mode where every modern retrieval surface (`memory.search`, `tree.*`, MCP tools) returned zero Notion results even with the toolkit connected.
- **Breaking risk**: Low — self-contained to Notion provider, `persist_single_item` stays for other callers, source_id format change is internal dedup-key only.
- **Security risk**: Zero — no auth surface, no user-facing input, no secrets.
- **Bottom line**: Safe to merge once CI is green.
