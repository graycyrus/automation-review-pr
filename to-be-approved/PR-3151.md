# PR #3151 — fix(subagent): box-pin engine recursion to fix tokio worker stack overflow
- **Author**: @CodeGhost21
- **Branch**: fix/e2e-lane1-subagent-shard-isolation → main
- **Created**: 2026-06-01
- **URL**: https://github.com/tinyhumansai/openhuman/pull/3151
- **Status**: clean
- **Last reviewed commit**: c6c98161d5bfa01a00b91667bf56675421ee4f19
- **Last review date**: 2026-06-01T16:17:17Z

## AI Summary
- **What it does**: Adds `Box::pin()` at two recursive async boundaries in `run_typed_mode` and `run_inner_loop` inside `src/openhuman/agent/harness/subagent_runner/ops.rs` to heap-allocate child engine futures and prevent tokio worker stack overflow when a parent agent delegates to a sub-agent. Also adds a regression smoke test exercising the nested dispatch on a constrained 1 MiB worker stack.
- **Breaking risk**: Zero — `Box::pin` changes allocation location, not behavior or API surface.
- **Security risk**: Zero — no auth, secrets, network, or input surfaces touched.
- **Bottom line**: Safe to merge once CI is fully green — code is correct, all prior [major] findings addressed. Waiting on lane 1/4 (Playwright E2E) and Rust Core Coverage to clear.

## UI Impact
N/A — Rust core only. No frontend files touched.

## Review History

### Review 1 — 2026-06-01T13:25:05Z
**Type**: Fresh
**Commit**: c35e94930144cd3e10c0bfc55ea59d1839132d9b
**Summary**: PR wraps two recursive async calls with `Box::pin` in the subagent runner to fix a tokio stack overflow on the parent→child engine dispatch path. Changes are minimal (4 lines of actual code) and structurally correct. Adds one new Rust unit test.
**Gates**: CI FAILING — E2E lanes 1/4 and 2/4 fail, Rust E2E (mock backend) fail, Frontend Coverage fail, Rust Core Coverage fail. No merge conflicts.
**Areas changed**: Rust core — `subagent_runner/ops.rs`, `subagent_runner/ops_tests.rs`
**Red flags**: Rust E2E (mock backend) failing on a Rust-only change is unexplained. Lane 1/4 still failing despite this PR claiming to fix the lane 1/4 cascade. `--no-verify` bypass on initial push documented in PR metadata.
**Linked issues**: None (investigation-driven from observed CI failures)
**PR-Issue alignment**: N/A — no linked issue
**CodeRabbit dedup**: CodeRabbit approved, no inline comments. Nothing to dedup.
**Resolution actions**: None
**Surrounding code patterns checked**: None needed — diff is self-contained and the Box::pin pattern is standard Rust async practice.
**Dependency audit**: N/A — no dependency changes
**Test coverage**: New smoke test `nested_subagent_dispatch_runs_on_a_constrained_worker_stack` added. Test is honest about its limitations (unit-level stack frames are much smaller than production). Coverage gate is in `skipping` state due to upstream coverage job failures.
**Impact scan**: N/A — no exported types or signatures changed. `Box::pin` wrapping is invisible to callers.
**ai_slop**: false — PR explicitly AI-authored, but the code change is targeted and correct. Verbose PR description is AI-generated but technically accurate.
**Findings**:
- [major] `Rust E2E (mock backend)` is failing — this is the Rust integration check most directly relevant to the changed code path. Validation section in the PR only covers `cargo test --lib openhuman::agent::harness::subagent_runner`. Need confirmation this failure is pre-existing on main, not introduced by this PR.
- [major] E2E lanes 1/4 and 2/4 still failing — the PR's stated purpose is to fix the lane 1/4 cascade from the stack overflow. If the fix works, lane 1/4 should be greener. Author needs to explain which remaining failures are independent of the fixed crash.
- [minor] `--no-verify` bypass on initial push — documented in PR metadata as "per CLAUDE.md guidance" but the root cause was stale local `node_modules`. The right fix was `pnpm install`, which they did on the second push. Mentioning it because hooks should not be treated as optional.
**Action taken**: COMMENT posted — asked author to explain Rust E2E failure, clarify lane 1/4 status, and resolve CI before re-review.
**GitHub review URL**: https://github.com/tinyhumansai/openhuman/pull/3151#pullrequestreview-4401544937

### Review 2 — 2026-06-01T16:17:17Z

#### Context
| Field | Value |
|-------|-------|
| Type | Continuation |
| Commit | c6c98161d5bfa01a00b91667bf56675421ee4f19 |
| Gates | CI FAILING (lane 1/4 Playwright E2E) \| Rust Core Coverage PENDING \| No conflicts |
| Areas changed | Rust core — `subagent_runner/ops.rs`, `session/turn.rs` |
| Linked issues | None |
| PR-Issue alignment | N/A |

#### Analysis
| Field | Value |
|-------|-------|
| Summary | New commit wraps the entire `run_subagent` body in `Box::pin(async move { ... }).await` and box-pins the parent agent's `run_turn_engine` call in `session/turn.rs:545`. Correctly identified that the first commit's inner boxes weren't reached because the overflow happened in the parent's poll frame on the way IN to `run_subagent`. Both changes move large state machine frames off the tokio worker stack at the right boundaries. |
| CodeRabbit dedup | CodeRabbit re-approved with no inline comments. Nothing to dedup. |
| Resolution actions | No threads from prior review — first review was a body-only COMMENT. |
| Surrounding code checked | None needed — change is a pure wrap, all logic preserved. |
| Dependency audit | N/A |
| Test coverage | All 45 subagent_runner tests still pass including the regression smoke. Rust Core Coverage is pending. |
| Impact scan | N/A — no exported types or signatures changed. |

#### Prior findings resolution
- [major] Rust E2E (mock backend) failing → FIXED — now passes.
- [major] E2E lanes 1/4 and 2/4 failing → lane 2/4 now passes. Lane 1/4 still failing (Playwright, not Rust — potentially pre-existing flaky).
- [minor] --no-verify bypass → no code fix needed, cosmetic note only.

#### Findings
None — code is clean. Only blocker is CI.

#### Outcome
| Field | Value |
|-------|-------|
| Action taken | COMMENT — code is clean and prior [major] findings addressed. CI not fully green: lane 1/4 (Playwright E2E) still failing, Rust Core Coverage pending. Asked author to confirm lane 1/4 is pre-existing flaky on main. Will approve once CI is fully green. |
| GitHub review URL | https://github.com/tinyhumansai/openhuman/pull/3151#pullrequestreview-4402837546 |
